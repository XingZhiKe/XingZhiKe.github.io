<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>LLDB详解</title>
    <url>/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="一、引言"><a href="#一、引言" class="headerlink" title="一、引言"></a>一、引言</h1><p>作为程序员，日常编写代码的时间远小于调试的时间，所以熟练掌握调试工具能够大幅提高工作效率。本文针对iOS开发过程中通过Xcode使用LLDB进行调试进行的讲解，以有提升自己对此知识的掌握。如果同时能够帮助到广大网友，那么我讲十分开心。</p>
<p>在日常工作中，大部分调试动作都是设置一些断点，在程序被暂停时查看执行逻辑、变量的值等，然后定位问题，并根据具体问题进行修改。这应该是大部分程序员的日常调试行为与状态。此种调试行为仅使用LLDB所提供的极少的能力。并且，在大部分情况下，还有不少程序员会借助在源代码里添加NSLog之类的日志代码，来进行辅助调试。虽然以上调试行为是必要的、也解决了不少的问题，但是其十分笨拙与原始，并且效率实在是较低。</p>
<p>本文将对LLDB的部分不常用功能进行说明，有助于我们掌握这一强大的工具，尽可能的提升工作效率</p>
<span id="more"></span>



<h1 id="二、LLDB命令"><a href="#二、LLDB命令" class="headerlink" title="二、LLDB命令"></a>二、LLDB命令</h1><p>LLDB是开源的内置于XCode的具有REPL(read-eval-print-loop)特征的调试器，其提供C++、Python接口。在日常的开发和调试过程中给开发人员带来了非常多的帮助。在Xcode5.0的时候，LLDB全面取代了GDB，使调试工作更加高效、便捷。LLDB是LLVM的一部分。本节重点讲述LLDB的命令，这些命令大部分可以可通过Xcode提的GUI界面来处理，也可以使用Xcode提供的调试控制台以交互命令的方式执行。</p>
<div style="width:70%;margin:auto"><img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/gui.jpg" class="" title="slug 标题"></div>
<div style="width:70%;margin:auto"><img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/condition.png" class="" title="修改变量的值 标题"></div>

<h2 id="2-1-命令概述"><a href="#2-1-命令概述" class="headerlink" title="2.1 命令概述"></a>2.1 命令概述</h2><p>LLDB命令的格式格式如下，其中[]表示是可选部分。</p>
<p><code>&lt;command&gt; [&lt;subcommand&gt; [&lt;subcommand&gt;...]] &lt;action&gt; [-options [option-value]] [argument[argument...]]</code></p>
<ul>
<li>command<ul>
<li>基于LLDB运行环境创建执行命令所需要的上下文环境</li>
</ul>
</li>
<li>subcommand<ul>
<li>在command创建的上下文环境中进行修改，形成更具体的新的上下文子命令以空格分隔，用于组织相关操作breakpoint和删除breakpoint</li>
</ul>
</li>
<li>action<ul>
<li>在上下文环境中需要执行的动作</li>
</ul>
</li>
<li>options<ul>
<li>动作所需要的选项</li>
<li>一个命令可能包含一个或多个选项 (option) 。选项以空格分隔，以双短划线 (–)开头，组合修改要执行的操作。有些option还提供使用单个短划线 (-) 的简写形式。例如，第一次导致程序停止后自动删除breakpoint的breakpoint set –one-shot命令，可以写为breakpoint set -o。</li>
<li>有些option还会传递值，option和值用空格分隔。例如，为breakpoint set命令传递–name option，选项值为函数、方法名称，这样就可以为指定函数、方法添加breakpoint。</li>
<li>有些命令组合使用options。例如，breakpoint set命令组合使用–file和–line选项，并为其指定对应文件名称和行号。</li>
<li>Option values和arguments中的空格用双引号保护。如果参数中有双引号，则可以使用反斜杠()标志双引号。LLDB等效的使用单引号和双引号。例如：<br><code>（lldb）command [subcommand] -option &quot;some \&quot;quoted\&quot; string&quot;</code><br>也可以写为：<br><code> (lldb) command [subcommand] -option &#39;some &quot;quoted&quot; string&#39;</code><br>这样的命令解析设计有助于LLDB命令语法在所有命令中保持统一。</li>
<li>如果命令即接受options，又接受自由形式 (free form) 的arguments，则必须在最后一个option和第一个argument间放置以空格分隔的双短划线，如expression命令。这样能够确保以短划线开头的argument被解释为argument，而不会因其与option都以短划线开头而被错误解释。</li>
</ul>
</li>
<li>arguement<ul>
<li>整个命令所需要参数</li>
<li>一个命令可能需要一个或多个参数，参数以空格分隔，用于指示要执行的操作。例如，breakpoint disable命令参数传递要禁用的breakpoint。breakpoint disable 1 2表示禁用breakpoint ID为1和2的断点。</li>
</ul>
</li>
</ul>
<h3 id="2-2-1-命令形式"><a href="#2-2-1-命令形式" class="headerlink" title="2.2.1 命令形式"></a>2.2.1 命令形式</h3><p>相同的命令可以用不同的形式实现<br>例如，以下命令表示同一操作：验证变量someVariable描述，并输出结果。</p>
<ul>
<li>规范形式 canonical form：expression –object-description – someVariable。</li>
<li>缩写形式 abbreviated form：e -O – someVariable。</li>
<li>别名 alias：po someVariable。可以为任何命令创建别名，以方便执行常见操作。</li>
</ul>
<h3 id="2-2-2-命令补全-Command-Completion"><a href="#2-2-2-命令补全-Command-Completion" class="headerlink" title="2.2.2 命令补全(Command Completion)"></a>2.2.2 命令补全(Command Completion)</h3><p>LLDB支持源文件名，符号名，文件名，等等的命令补全(Commmand Completion)。终端窗口中的补全是通过在命令行中输入一个制表符来初始化的。Xcode控制台中的补全与在源码编辑器中的补全方式是一样的：补全会在第三个字符被键入时自动弹出，或者通过Esc键手动取消弹出。</p>
<p>一个命令中的私有选项可以有不同的完成者(completers)。如breakpoint中的–file <path>选项作为源文件的完成者，–shlib <path>选项作为当前加载的库的完成者，等等。这些行为是特定的，例如，如果指定–shlib <path>，且以–file <path>结尾，则LLDB只会列出由–shlib <path>指定的共享类库。</p>
<h2 id="2-2-命令详解"><a href="#2-2-命令详解" class="headerlink" title="2.2 命令详解"></a>2.2 命令详解</h2><h3 id="2-2-1-帮助类命令"><a href="#2-2-1-帮助类命令" class="headerlink" title="2.2.1 帮助类命令"></a>2.2.1 帮助类命令</h3><h4 id="2-2-1-1-help"><a href="#2-2-1-1-help" class="headerlink" title="2.2.1.1 help"></a>2.2.1.1 help</h4><p>help命令可以查看LLDB的帮助文档，方便进一步了解LLDB命令结构。<br>只输入help命令，将返回所有命令列表，如下是部分命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Debugger commands:</span><br><span class="line">  apropos           -- List debugger commands related to a word or subject.</span><br><span class="line">  breakpoint        -- Commands for operating on breakpoints (see &#x27;help b&#x27; for</span><br><span class="line">                       shorthand.)</span><br><span class="line">  command           -- Commands for managing custom LLDB commands.</span><br><span class="line">  disassemble       -- Disassemble specified instructions in the current</span><br><span class="line">                       target.  Defaults to the current function for the</span><br><span class="line">                       current thread and stack frame.</span><br><span class="line">  expression        -- Evaluate an expression on the current thread.  Displays</span><br><span class="line">                       any returned value with LLDB&#x27;s default formatting.</span><br><span class="line">  frame             -- Commands for selecting and examing the current thread&#x27;s</span><br><span class="line">                       stack frames.</span><br><span class="line">  gdb-remote        -- Connect to a process via remote GDB server.</span><br><span class="line">                       If no host is specifed, localhost is assumed.</span><br><span class="line">                       gdb-remote is an abbreviation for &#x27;process connect</span><br><span class="line">                       --plugin gdb-remote connect://&lt;hostname&gt;:&lt;port&gt;&#x27;</span><br><span class="line">  gui               -- Switch into the curses based GUI mode.</span><br></pre></td></tr></table></figure>
<p>可以通过<code>help command</code>来查看命令的具体帮助信息</p>
<h4 id="2-2-1-2-apropos"><a href="#2-2-1-2-apropos" class="headerlink" title="2.2.1.2 apropos"></a>2.2.1.2 apropos</h4><p>当我们只记得某个单词，但是不太确定属于某个命令、子命令等时，可以通过此命令进行查找</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(lldb) apropos jump</span><br><span class="line">The following commands may relate to &#x27;jump&#x27;:</span><br><span class="line">  _regexp-jump -- Set the program counter to a new address.</span><br><span class="line">  thread jump  -- Sets the program counter to a new address.</span><br><span class="line">  jump         -- Set the program counter to a new address.</span><br></pre></td></tr></table></figure>
<h3 id="2-2-2-expression"><a href="#2-2-2-expression" class="headerlink" title="2.2.2 expression"></a>2.2.2 expression</h3><p>这个命令可能是我们最常用的命令了，我们经常使用的是po、p，就是它的简写</p>
<h4 id="2-2-2-1-po"><a href="#2-2-2-1-po" class="headerlink" title="2.2.2.1 po"></a>2.2.2.1 po</h4><p>po命令在当前线程上执行表达式，并输出表达式的计算结果。此命令其实是<code>expression -O  --</code>的简写形式，其中<code>-O</code>实际上是<code>--object-description</code>，此选项表示使用类型提供的描述来输出结果，对于OC语言来说就是descriprtion。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;po&#x27; is an abbreviation for &#x27;expression -O  --&#x27;</span><br><span class="line"></span><br><span class="line">-O ( --object-description )</span><br><span class="line">            Display using a language-specific description API, if possible.</span><br></pre></td></tr></table></figure>
<p>其使用非常简单，但是其背后的原理比较复杂。</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/po.png" class="" title="po的背后原理">

<h4 id="2-2-2-2-p"><a href="#2-2-2-2-p" class="headerlink" title="2.2.2.2 p"></a>2.2.2.2 p</h4><p>p命令在当前线程上执行表达式，并输出表达式的计算结果，此结果以LLDB内置的格式标识。此命令其实是<code>expression --</code>的简写形式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;p&#x27; is an abbreviation for &#x27;expression --&#x27;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>其使用非常简单，但是其背后的原理比较复杂。</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/p.png" class="" title="p的背后原理">

<h4 id="2-2-2-3-v"><a href="#2-2-2-3-v" class="headerlink" title="2.2.2.3 v"></a>2.2.2.3 v</h4><p>此命令不属于expression，但是其与po、p作用类似，所以一并在这里介绍。<br>显示当前堆栈帧的变量。<br>默认为所有参数和当前范围内的变量。参数名称、局部名称、静态文件名称和全局文件名称可以指定变量。 可以指定聚合变量的子项例如“var-&gt;child.x”。 ‘frame variable’ 中的 -&gt; 和 [] 运算符不调用运算符重载（如果存在），但直接访问指定的元素。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;v&#x27; is an abbreviation for &#x27;frame variable&#x27;</span><br></pre></td></tr></table></figure>
<p>其使用非常简单，但是其背后的原理比较复杂。</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/v.png" class="" title="p的背后原理">

<p>以下是po、p、v的对比</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/pv-p-v.png" class="" title="对比">

<h3 id="2-2-2-breakpoint"><a href="#2-2-2-breakpoint" class="headerlink" title="2.2.2 breakpoint"></a>2.2.2 breakpoint</h3><p>此命令是与用于设置、取消、修改断点等操作。</p>
<p>使用breakpoint 的子命令 set 设置断点<br>具体信息可以通过help breakpoint set 查询所有可选的选项(options)以及参数(arguments).</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 按方法名称设置--name(-n):</span><br><span class="line">(lldb) breakpoint set -n greet</span><br><span class="line">Breakpoint 2: where = Greeter`Greeter.Greeter.greet(personNamed: Swift.String) -&gt; () + 127 at Greeter.swift:9:12, address = 0x00000001000034cf</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 按行数设置--line(-l):</span><br><span class="line">(lldb) breakpoint set -l 18</span><br><span class="line">Breakpoint 3: where = Greeter`main + 14 at Greeter.swift:18:15, address = 0x000000010000315e</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">按文件设置--file(-f):</span><br><span class="line">(lldb) breakpoint set -f Greeter.swift -l 20</span><br><span class="line">Breakpoint 6: where = Greeter`main + 34 at Greeter.swift:20:1, address = 0x0000000100003172</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 根据语言捕获异常--language-exception(-E):</span><br><span class="line">(lldb) breakpoint set -E Objc</span><br><span class="line">Breakpoint 7: no locations (pending).</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 可以通过--exception-typename(-O) 指定要捕获的异常类型:</span><br><span class="line">(lldb)  breakpoint set -E Swift -O EnumErrorType</span><br><span class="line">Breakpoint 8: no locations (pending).</span><br><span class="line">// pending，说明某块还没有被加载</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 获取断点列表: breakpoint list</span><br><span class="line">(lldb) breakpoint list</span><br><span class="line">Current breakpoints:</span><br><span class="line">1: name = &#x27;sayHello&#x27;, locations = 0 (pending)</span><br><span class="line"></span><br><span class="line">2: name = &#x27;greet&#x27;, locations = 1</span><br><span class="line">  2.1: where = Greeter`Greeter.Greeter.greet(personNamed: Swift.String) -&gt; () + 127 at Greeter.swift:9:12, address = Greeter[0x00000001000034cf], unresolved, hit count = 0</span><br><span class="line"></span><br><span class="line">3: file = &#x27;/Users/jackli/Desktop/Roborock/Greeter.swift&#x27;, line = 18, exact_match = 0, locations = 1</span><br><span class="line">  3.1: where = Greeter`main + 14 at Greeter.swift:18:15, address = Greeter[0x000000010000315e], unresolved, hit count = 0</span><br><span class="line"></span><br><span class="line">4: file = &#x27;main.swift&#x27;, line = 20, exact_match = 0, locations = 0 (pending)</span><br><span class="line"></span><br><span class="line">5: file = &#x27;Greet.swift&#x27;, line = 20, exact_match = 0, locations = 0 (pending)</span><br><span class="line"></span><br><span class="line">6: file = &#x27;Greeter.swift&#x27;, line = 20, exact_match = 0, locations = 1</span><br><span class="line">  6.1: where = Greeter`main + 34 at Greeter.swift:20:1, address = Greeter[0x0000000100003172], unresolved, hit count = 0</span><br><span class="line"></span><br><span class="line">7: Exception breakpoint (catch: off throw: on) the correct runtime exception handler will be determined when you run</span><br><span class="line"></span><br><span class="line">8: Swift Error breakpoint the correct runtime exception handler will be determined when you run</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">需要注意的是: 设置断点其实是设置了逻辑断点, 可能会对应一个或多个位置断点(location).</span><br><span class="line">每个逻辑断点都有一个按照顺序从1开始分配的整型ID. 每一个位置断点又有一个自己的位置ID, 与逻辑断点的ID之间以.分隔, 用于定位位置断点. 示例如下:</span><br><span class="line">2: name = &#x27;greet&#x27;, locations = 1</span><br><span class="line">  2.1: where = Greeter`Greeter.Greeter.greet(personNamed: Swift.String) -&gt; () + 127 at Greeter.swift:9:12, address = Greeter[0x00000001000034cf], unresolved, hit count = 0</span><br><span class="line"></span><br><span class="line">// 逻辑断点是动态的, 如果程序载入了新的代码, 会自动载入新的位置断点.</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 修改断点:</span><br><span class="line">// breakpoint modify</span><br><span class="line">// 使用这个命令修改逻辑断点或者位置断点, 需要传递逻辑断点或者位置断点的ID 作为参数, 具体参数可以通过help breakpoint modify 查看.</span><br><span class="line"></span><br><span class="line">部分常用选项如下:</span><br><span class="line">--condition(-c) 传入表达式参数, 只有表达式判断为true 才会执行断点.</span><br><span class="line">ignore-count(-i) 指定跳过断点的次数, 在该断点处跳过指定次数后, 才会执行该断点.</span><br><span class="line">--one-shot(-o) 执行断点一次后移除该断点.</span><br><span class="line">--queue-name(-q) 指定队列名称, 该断点只有在指定队列上才会执行.</span><br><span class="line">--thread-name(-T) 指定线程名称, 该断点只有在指定的线程上才会执行.</span><br><span class="line">--thread-id(-t) 指定线程ID(TID), 该断点只有在指定的线程上才会执行.</span><br><span class="line">--thread-index(-x) 指定线程index, 该断点只有在指定的线程上才会执行.</span><br><span class="line"></span><br><span class="line">如下示例修改了断点ID为1的断点, 指定其在执行后,自动移除.</span><br><span class="line">(lldb) breakpoint modify --one-shot 1</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">我们都知道, 当程序抵达断点处时, 程序会暂停执行, 此时可以执行lldb命令.</span><br><span class="line">我们也可以通过执行breakpoint command add 命令, 让程序每次抵达断点处时, 自动执行我们添加的命令. 如:</span><br><span class="line">在位置断点1.1处添加指令, 每行可以添加一个指令, Enter 跳转至下一指令, 输入DONE结束添加.</span><br><span class="line">(lldb) breakpoint command add 1.1</span><br><span class="line">Enter your debugger command(s). Type &#x27;DONE&#x27; to end.</span><br><span class="line">&gt; thread backtrace</span><br><span class="line">&gt; DONE</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">可以将process continue 作为最后一个指令, 那么调试器就会在执行指令后, 自动继续执行程序, 这在记录Log的场景下使用是非常方便的.</span><br><span class="line">(lldb) target create &quot;Greeter&quot;</span><br><span class="line">Current executable set to (x86_64).</span><br><span class="line">(lldb) breakpoint list</span><br><span class="line">No breakpoints currently set.</span><br><span class="line">(lldb) breakpoint set -n greet</span><br><span class="line">Breakpoint 1: where = Greeter`Greeter.Greeter.greet(personNamed: Swift.String) -&gt; () + 127 at Greeter.swift:9:12, address = 0x00000001000034cf</span><br><span class="line">(lldb) breakpoint command add 1</span><br><span class="line">Enter your debugger command(s).  Type &#x27;DONE&#x27; to end.</span><br><span class="line">&gt; frame variable</span><br><span class="line">&gt; process continue</span><br><span class="line">(lldb) process launch</span><br><span class="line">Process 5997 launched: (x86_64)</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">设置断点可用状态(Diable/Enable)</span><br><span class="line">禁用断点: breakpoint disable</span><br><span class="line">启用断点: breakpoint enable</span><br><span class="line">以上命令都需要指定断点ID参数(支持通配符).</span><br><span class="line">当逻辑断点设置为diable时, 它下方的所有位置断点都将不再生效.</span><br><span class="line">(lldb) breakpoint disable 1</span><br><span class="line">1 breakpoints disabled.</span><br><span class="line">(lldb) breakpoint disable 2.1</span><br><span class="line">1 breakpoints disabled.</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">(lldb) breakpoint enable 1</span><br><span class="line">1 breakpoints enabled.</span><br><span class="line">(lldb) breakpoint enable 2.1</span><br><span class="line">1 breakpoints enabled.</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">还可用通配符*指定断点.</span><br><span class="line">如下示例先是禁用了逻辑断点1下的所有位置断点, 而后启用了位置断点1.1</span><br><span class="line">(lldb) breakpoint disable 1.*</span><br><span class="line">2 breakpoint disabled*</span><br><span class="line">(lldb) breakpoint enable 1.1</span><br><span class="line">1 breakpoint enabled.</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">删除断点</span><br><span class="line">(lldb) breakpoint delete 1</span><br><span class="line">1 breakpoints deleted; 2 breakpoint locations disabled.</span><br></pre></td></tr></table></figure>
<h3 id="2-2-3-watchpoint"><a href="#2-2-3-watchpoint" class="headerlink" title="2.2.3 watchpoint"></a>2.2.3 watchpoint</h3><p>其与breakpoint很类似，主要用于在感知到内存发生变化后触发断点。内存断点受硬件上运行被调试程序的寄存器数量的限制。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 针对变量设置内存断点</span><br><span class="line">(lldb) watchpoint set variable places</span><br><span class="line">Watchpoint created: Watchpoint 1: addr = 0x100004a40 size = 8 state = enabled type = w</span><br><span class="line">    declare @ &#x27;main.swift:7&#x27;</span><br><span class="line">    watchpoint spec = &#x27;places&#x27;</span><br><span class="line">    new value: 1 value</span><br><span class="line"></span><br><span class="line">// 针对表达式返回值设置内存断点</span><br><span class="line">(lldb) watchpoint set expression -- (int *)$places + 8</span><br><span class="line">Watchpoint created: Watchpoint 2: addr = 0x100005f33 size = 8 state = enabled type = w</span><br><span class="line">    new value: 0x0000000000000000</span><br><span class="line"></span><br><span class="line">// 默认情况下，内存断点监视对变量或地址的写访问。 通过传递值为 read、write 或 read_write 的 --watch (-w) 选项来指定对监视器的访问类型。</span><br><span class="line"></span><br><span class="line">// 默认情况下，观察点使用目标机器的指针字节大小监视读取和写入。 通过传递值为 1、2、4 或 8 的 --size (-s) 选项，更改用于监视区域的字节数。</span><br></pre></td></tr></table></figure>
<p>内存断点的删除、修改等操作同breakpoint</p>
<h3 id="2-2-4-thread"><a href="#2-2-4-thread" class="headerlink" title="2.2.4 thread"></a>2.2.4 thread</h3><ul>
<li>n 下一行（next的简写）</li>
<li>c 继续（continue的简写）</li>
<li>s 进入函数（step into的简写）</li>
<li>finish 跳出当前函数<br>上面是命令行的输入，对应下面的GUI按钮。<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/flowcontroller.png" class="" title="流程控制">
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 返回某值</span><br><span class="line">thread return value </span><br><span class="line"></span><br><span class="line">// 跳过2条语句</span><br><span class="line">thread jump --by 2</span><br><span class="line"></span><br><span class="line">// 这两个命令也与流程控制类似，不过它们属于frame</span><br><span class="line">up\down</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="2-2-5-image"><a href="#2-2-5-image" class="headerlink" title="2.2.5 image"></a>2.2.5 image</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 列出所有依赖库</span><br><span class="line"></span><br><span class="line">(lldb) image list</span><br><span class="line">[  0] 69F6C476-555D-3987-B162-E62C9A1D718C 0x000000010008c000 /var/mobile/Containers/Bundle/Application/D6B51B8B-00F8-443F-9B8E-EF47C2330D2B/WhatsApp.app/WhatsApp (0x000000010008c000)</span><br><span class="line">[  1] 3134CFB2-F722-310E-A2C7-42AE4DC131AB 0x00000001017ec000 /Library/MobileSubstrate/MobileSubstrate.dylib (0x00000001017ec000)</span><br><span class="line">[  2] 4DB79D94-6764-3180-99C9-5615E29073E7 0x0000000196848000 /Users/piao/Library/Developer/Xcode/iOS DeviceSupport/9.0.2 (13A452)/Symbols/usr/lib/libSystem.B.dylib</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 查找ASLR偏移</span><br><span class="line">(lldb) image list -f -o WhatsApp</span><br><span class="line">[  0] /var/mobile/Containers/Bundle/Application/D6B51B8B-00F8-443F-9B8E-EF47C2330D2B/WhatsApp.app/WhatsApp 0x000000000008c000(0x000000010008c000)</span><br><span class="line">ASLR偏移：0x000000000008c000</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 打印加载模块列表</span><br><span class="line"></span><br><span class="line">(lldb) image list -f -o // 通常带这两个参数使用</span><br><span class="line">[  0] 40E417A4-F966-3DB4-B028-B0272DC016A7 0x000a0000 /Users/piao/Library/Developer/Xcode/DerivedData/debug-bdkhskdqykkoqmhjedilckzvpuls/Build/Products/Debug-iphoneos/debug.app/debug </span><br><span class="line">      /Users/piao/Library/Developer/Xcode/DerivedData/debug-bdkhskdqykkoqmhjedilckzvpuls/Build/Products/Debug-iphoneos/debug.app.dSYM/Contents/Resources/DWARF/debug</span><br><span class="line">[  1] 011601C0-F561-3306-846B-94A7C8C841DA 0x2d9e6000 /Users/piao/Library/Developer/Xcode/iOS DeviceSupport/7.1.2 (11D257)/Symbols/System/Library/Frameworks/CoreGraphics.framework/CoreGraphics</span><br><span class="line">查找原始地址信息</span><br><span class="line">在可执行文件或任何共享库中查找原始地址的信息。</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// image lookup -a 表达式</span><br><span class="line">例子：</span><br><span class="line">(lldb) image lookup -a $pc</span><br><span class="line">Address: debug[0x0000b236] (debug.__TEXT.__text + 1254)</span><br><span class="line">Summary: debug`main + 58 at main.m:16</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//查找类信息</span><br><span class="line">image lookup --type UIViewController</span><br><span class="line">image lookup -t UIViewController</span><br><span class="line"></span><br><span class="line">//查找符号</span><br><span class="line">image lookup --name viewDidLoad</span><br><span class="line">image lookup -n viewDidLoad</span><br><span class="line"></span><br><span class="line">//查找地址</span><br><span class="line">image lookup --address 0x000000018ff60490</span><br><span class="line">image lookup -a 0x000000018ff60490</span><br></pre></td></tr></table></figure>

<h1 id="三、使用LLDB"><a href="#三、使用LLDB" class="headerlink" title="三、使用LLDB"></a>三、使用LLDB</h1><p>上面从语法角度讲述了LLDB所提供的部分命令以及命令的用法，下面从实际工作的角度来说明，为什么需要LLDB来提升工作效率。<br>在工程较小的情况下，发现问题，修改代码，运行以查看结果是否正确，这个过程耗费的时间不大。但是在工程量比较大的情况下，即使仅仅修改一行代码，那么编译、运行所花费的时间也可能有3分钟左右。假如，每天做30次这样的动作，那么耗费的时间将是90分钟，这个时间还是十分惊人的！！！<br>另外，我们在调试时对代码的修改可能是临时性的，最好能经过验证、并且尽可能的排除所有的问题后，再对代码进行正式修改。这些都需要借助LLDB的神奇魔力。<br>以下说明以Xcode提供的GUI和命令窗口来实现。我们的大部分操作都是可以通过如下图所示的界面来完成的。</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/edit-breakpoint.png" class="" title="修改断点">

<h2 id="3-1-修改返回值"><a href="#3-1-修改返回值" class="headerlink" title="3.1 修改返回值"></a>3.1 修改返回值</h2><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="type">int</span>)rtInt &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;rtInt&quot;</span>);</span><br><span class="line">    <span class="comment">// 在下面一行设置断点，并添加如下图所示内容(方法1)</span></span><br><span class="line">    <span class="type">int</span> n = <span class="number">10</span>;</span><br><span class="line">    <span class="comment">// 在下面一行设置断点，并添加如下图所示内容(方法2)</span></span><br><span class="line">    <span class="keyword">return</span> n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/rtint.png" class="" title="返回整数">
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/rtint2.png" class="" title="返回整数">

<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="built_in">NSDictionary</span> *)rtDict &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;rtDict&quot;</span>);</span><br><span class="line">    <span class="built_in">NSDictionary</span> *dict= @&#123;<span class="string">@&quot;key&quot;</span>:<span class="string">@&quot;value&quot;</span>&#125;;</span><br><span class="line">    <span class="comment">// 设置断点，然后控制台输入，并在LLDB的环境里定义变量，并把变量赋值给我们需要的对象</span></span><br><span class="line">    <span class="comment">// e NSDictionary *$otherDict = @&#123;@&quot;key1&quot;:@&quot;value1&quot;&#125;</span></span><br><span class="line">    <span class="comment">// e dict = $otherDict</span></span><br><span class="line">    <span class="keyword">return</span> dict;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="3-2-输出布局信息"><a href="#3-2-输出布局信息" class="headerlink" title="3.2 输出布局信息"></a>3.2 输出布局信息</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">po [self.view recursizeDescription]</span><br><span class="line">error: Execution was interrupted, reason: Attempted to dereference an invalid ObjC Object or send it an unrecognized selector.</span><br><span class="line">The process has been returned to the state before expression evaluation.</span><br></pre></td></tr></table></figure>
<p>以上只是简单的UI调试，可以使用Xcode提供的Debug View Hierarchy来进行更深入的排查。</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/gui-debug.png" class="" title="特定次数才需要暂停">

<h2 id="3-3-修改UI并实时看到"><a href="#3-3-修改UI并实时看到" class="headerlink" title="3.3 修改UI并实时看到"></a>3.3 修改UI并实时看到</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- (void)viewDidLoad &#123;</span><br><span class="line">    [super viewDidLoad];</span><br><span class="line">    // Do any additional setup after loading the view.</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    UIButton *btn = [[UIButton alloc] initWithFrame:CGRectMake(10, 10, 200, 80)];</span><br><span class="line">    [self.view addSubview:btn];</span><br><span class="line">    [btn addTarget:self action:@selector(click:) forControlEvents:UIControlEventTouchUpInside];</span><br><span class="line">    [btn setTitleColor:UIColor.redColor forState:UIControlStateNormal];</span><br><span class="line">    [btn setTitle:@&quot;点我&quot;forState:UIControlStateNormal];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (void)click:(UIButton *)btn &#123;</span><br><span class="line">    // 在这里设断定，并输入如下命令</span><br><span class="line">    // e (void)[self.view setBackgroundColor:[UIColor grayColor]] 修改颜色</span><br><span class="line">    // e (void)[CATransaction flush]  // 把颜色的修改通过渲染更新到界面</span><br><span class="line">    NSLog(@&quot;here&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意<br>以上命令并不总是能成功，在viewDidLoad中设置断点然后进行操作就不行</p>
</blockquote>
<h2 id="3-4-注意返回类型"><a href="#3-4-注意返回类型" class="headerlink" title="3.4 注意返回类型"></a>3.4 注意返回类型</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在上面例子中，如果输入</span><br><span class="line">e [self.view setBackgroundColor:[UIColor grayColor]]</span><br><span class="line">则会报这个错误</span><br><span class="line">error: expression failed to parse:</span><br><span class="line">error: &lt;user expression 0&gt;:1:12: no known method &#x27;-setBackgroundColor:&#x27;; cast the message send to the method&#x27;s return type</span><br><span class="line">[self.view setBackgroundColor:[UIColor grayColor]]</span><br><span class="line">~~~~~~~~~~~^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~</span><br><span class="line"></span><br><span class="line">// 上面的错误信息告诉LLDB不知道返回类型，所以需要强制类型转换</span><br></pre></td></tr></table></figure>
<h2 id="3-5-跳过某些语句"><a href="#3-5-跳过某些语句" class="headerlink" title="3.5 跳过某些语句"></a>3.5 跳过某些语句</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- (void)viewDidLoad &#123;</span><br><span class="line">    [super viewDidLoad];</span><br><span class="line">    // Do any additional setup after loading the view.</span><br><span class="line">    </span><br><span class="line">    NSLog(@&quot;%d&quot;, __LINE__); // 假设此行是41</span><br><span class="line">    NSLog(@&quot;%d&quot;, __LINE__); // 在此行设置断点，thread jump --by 1</span><br><span class="line">    NSLog(@&quot;%d&quot;, __LINE__);</span><br><span class="line">    NSLog(@&quot;%d&quot;, __LINE__);</span><br><span class="line">    NSLog(@&quot;%d&quot;, __LINE__);</span><br><span class="line">    NSLog(@&quot;%d&quot;, __LINE__);</span><br><span class="line"></span><br><span class="line">    // 以下是输出，可以看出跳过了41行</span><br><span class="line">    baseStudy[33708:346268] 41</span><br><span class="line">    baseStudy[33708:346268] 43</span><br><span class="line">    baseStudy[33708:346268] 44</span><br><span class="line">    baseStudy[33708:346268] 45</span><br><span class="line">    baseStudy[33708:346268] 46</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="3-6-命令后执行"><a href="#3-6-命令后执行" class="headerlink" title="3.6 命令后执行"></a>3.6 命令后执行</h2><img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/ecmd.png" class="" title="命令后执行">

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 这是上面设置的命令</span><br><span class="line">// -C 表示后面可以添加新的命令</span><br><span class="line">// -G true标识断点不停止，直接运行</span><br><span class="line">br set -l 43 -f ViewController.m  -C &#x27;po @&quot;onthway&quot;&#x27; -G true</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 可以看到断点其实被设置3个（因为循环了3次）</span><br><span class="line">5: file = &#x27;ViewController.m&#x27;, line = 43, exact_match = 0, locations = 1, resolved = 1, hit count = 2 Options: enabled auto-continue </span><br><span class="line">    Breakpoint commands:</span><br><span class="line">      po @&quot;onthway&quot;</span><br><span class="line"></span><br><span class="line">  5.1: where = baseStudy`-[ViewController viewDidLoad] + 90 at ViewController.m:43:9, address = 0x000000010d564cba, resolved, hit count = 2 </span><br><span class="line"></span><br><span class="line">6: file = &#x27;ViewController.m&#x27;, line = 43, exact_match = 0, locations = 1, resolved = 1, hit count = 1 Options: enabled auto-continue </span><br><span class="line">    Breakpoint commands:</span><br><span class="line">      po @&quot;onthway&quot;</span><br><span class="line"></span><br><span class="line">  6.1: where = baseStudy`-[ViewController viewDidLoad] + 90 at ViewController.m:43:9, address = 0x000000010d564cba, resolved, hit count = 1 </span><br><span class="line"></span><br><span class="line">7: file = &#x27;ViewController.m&#x27;, line = 43, exact_match = 0, locations = 1, resolved = 1, hit count = 0 Options: enabled auto-continue </span><br><span class="line">    Breakpoint commands:</span><br><span class="line">      po @&quot;onthway&quot;</span><br><span class="line"></span><br><span class="line">  7.1: where = baseStudy`-[ViewController viewDidLoad] + 90 at ViewController.m:43:9, address = 0x000000010d564cba, resolved, hit count = 0 </span><br></pre></td></tr></table></figure>
<h2 id="3-7-执行一次的断点"><a href="#3-7-执行一次的断点" class="headerlink" title="3.7 执行一次的断点"></a>3.7 执行一次的断点</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 把上面的断点语句添加--one-shot true</span><br><span class="line">br set --one-shot true -l 43 -f ViewController.m   -C &#x27;po @&quot;onthway&quot;&#x27; -G true</span><br><span class="line"></span><br><span class="line">// 输出3次  ontheway</span><br><span class="line">3: file = &#x27;/Users/wangwenjun04/Desktop/myfolder/studyCode/baseStudy/baseStudy/ViewController.m&#x27;, line = 43, exact_match = 0, locations = 1, resolved = 1, hit count = 3</span><br><span class="line"></span><br><span class="line">  3.1: where = baseStudy`-[ViewController viewDidLoad] + 90 at ViewController.m:43:9, address = 0x0000000103ea0cba, resolved, hit count = 3 </span><br><span class="line"></span><br><span class="line">4: file = &#x27;/Users/wangwenjun04/Desktop/myfolder/studyCode/baseStudy/baseStudy/ViewController.m&#x27;, line = 50, exact_match = 0, locations = 1, resolved = 1, hit count = 1</span><br><span class="line"></span><br><span class="line">  4.1: where = baseStudy`-[ViewController viewDidLoad] + 199 at ViewController.m:50:22, address = 0x0000000103ea0d27, resolved, hit count = 1 </span><br><span class="line"></span><br><span class="line">// 此断点只其一次作用</span><br><span class="line">7: file = &#x27;ViewController.m&#x27;, line = 43, exact_match = 0, locations = 1, resolved = 1, hit count = 0 Options: enabled one-shot auto-continue </span><br><span class="line">    Breakpoint commands:</span><br><span class="line">      po @&quot;onthway&quot;</span><br><span class="line"></span><br><span class="line">  7.1: where = baseStudy`-[ViewController viewDidLoad] + 90 at ViewController.m:43:9, address = 0x0000000103ea0cba, resolved, hit count = 0 </span><br></pre></td></tr></table></figure>
<h2 id="3-8-变量为特定次数"><a href="#3-8-变量为特定次数" class="headerlink" title="3.8 变量为特定次数"></a>3.8 变量为特定次数</h2><p>假如有以下代码，当i的值是9的时候会出现问题。那么按照上述基本的调试方法，在if这一行设置断点，然后要通过8次的暂停才能在我们想要的地方停住。</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSInteger</span> count = array.count;</span><br><span class="line"><span class="keyword">for</span> (<span class="built_in">NSInteger</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (i % <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="comment">// 做一些事情</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 做另外一些事情</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/time.png" class="" title="特定次数才需要暂停">
<p>通过设置跳过前8次执行，LLDB自动在我们想要的时候暂停程序的执行。通过设置ingore的次数，我们的调率得到了提升。</p>
<h2 id="3-9-变量为特定的值"><a href="#3-9-变量为特定的值" class="headerlink" title="3.9 变量为特定的值"></a>3.9 变量为特定的值</h2><p>假如我们有如下代码:</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> printName(<span class="built_in">NSString</span> *name) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;name=%@&quot;</span>, name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> main(<span class="type">int</span> argc, <span class="keyword">const</span> <span class="type">char</span> * argv[]) &#123;    </span><br><span class="line">    printName(<span class="string">@&quot;jzm&quot;</span>);</span><br><span class="line">    printName(<span class="string">@&quot;xjp&quot;</span>);</span><br><span class="line">    printName(<span class="string">@&quot;hjt&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们的需求是在name是xjp时暂停，那么按照简单的调试方法，只能在NSLog那一行来设置断点，然后再每次断点停住的时候检查name的值是否是xjp。我们可以更家搞笑，通过设置条件断点的方式。</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/condition.png" class="" title="条件断点">


<h1 id="三、扩展LLDB"><a href="#三、扩展LLDB" class="headerlink" title="三、扩展LLDB"></a>三、扩展LLDB</h1><p>LLDB、Xcode提供了便捷的方式进行扩展，这些扩展都基于LLDB提供的接口。</p>
<h2 id="3-1-工程级扩展"><a href="#3-1-工程级扩展" class="headerlink" title="3.1 工程级扩展"></a>3.1 工程级扩展</h2><p>我们可以专门针对某个工程进行扩展，其方法很简单。新建一个用于扩展LLDB的python文件，然后选择edit scheme进行配置。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 修改提示语</span><br><span class="line">setting set prompt [wwj-lldb]$</span><br></pre></td></tr></table></figure>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/pop.png" class="" title="修改提示">
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 新建一个命令别名，用于刷新UI</span><br><span class="line">command alias wflush e (void)[CATransaction flush]</span><br></pre></td></tr></table></figure>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/wflush.png" class="" title="wflush">
<p>edit scheme进行如下配置</p>
<img src="/2022/12/10/LLDB%E8%AF%A6%E8%A7%A3/project-custom.png" class="" title="project-custom">
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">setting set prompt [wwj-lldb]$</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">command alias wflush e (void)[CATransaction flush]</span><br></pre></td></tr></table></figure>

<h2 id="3-2-系统级扩展"><a href="#3-2-系统级扩展" class="headerlink" title="3.2 系统级扩展"></a>3.2 系统级扩展</h2><p>在~&#x2F;.lldbinit文件中添加自定义命令，自定义命令与<a href="#hexo-%E5%B7%A5%E7%A8%8B%E7%BA%A7%E6%89%A9%E5%B1%95">跳转到本文锚点</a>相近。<br>FaceBook开源的<a href="https://en.wiktionary.org/wiki/chisel">chisel</a>就是采用的此种方式。<br>其提供了许多扩展命令。</p>
<table>
<thead>
<tr>
<th>Command</th>
<th>Description</th>
<th>iOS</th>
<th>OS X</th>
</tr>
</thead>
<tbody><tr>
<td>pviews</td>
<td>Print the recursive view description for the key window.</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>pvc</td>
<td>Print the recursive view controller description for the key window.</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>visualize</td>
<td>Open a <code>UIImage</code>, <code>CGImageRef</code>, <code>UIView</code>, <code>CALayer</code>, <code>NSData</code> (of an image), <code>UIColor</code>, <code>CIColor</code>, or <code>CGColorRef</code> in Preview.app on your Mac.</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>fv</td>
<td>Find a view in the hierarchy whose class name matches the provided regex.</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>fvc</td>
<td>Find a view controller in the hierarchy whose class name matches the provided regex.</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>show&#x2F;hide</td>
<td>Show or hide the given view or layer. You don’t even have to continue the process to see the changes!</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>mask&#x2F;unmask</td>
<td>Overlay a view or layer with a transparent rectangle to visualize where it is.</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>border&#x2F;unborder</td>
<td>Add a border to a view or layer to visualize where it is.</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>caflush</td>
<td>Flush the render server (equivalent to a “repaint” if no animations are in-flight).</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>bmessage</td>
<td>Set a symbolic breakpoint on the method of a class or the method of an instance without worrying which class in the hierarchy actually implements the method.</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>wivar</td>
<td>Set a watchpoint on an instance variable of an object.</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>presponder</td>
<td>Print the responder chain starting from the given object.</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>…</td>
<td>… and many more!</td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h2 id="3-3-高阶扩展"><a href="#3-3-高阶扩展" class="headerlink" title="3.3 高阶扩展"></a>3.3 高阶扩展</h2><p>通过LLVM工程自己编译LLDB代码（难度高，有兴趣的高手可以尝试并分享给大家）。</p>
<h1 id="四、参考资料"><a href="#四、参考资料" class="headerlink" title="四、参考资料"></a>四、参考资料</h1><ol>
<li><a href="https://www.jianshu.com/p/da2e78fbf7d6">全面剖析 LLDB 调试器</a></li>
<li><a href="https://github.com/pro648/tips/blob/master/sources/LLDB%E7%9A%84%E4%BD%BF%E7%94%A8.md">LLDB的使用</a></li>
<li><a href="http://southpeak.github.io/2015/01/25/tool-lldb/">LLDB调试器使用简介</a></li>
<li><a href="https://developer.apple.com/videos/play/wwdc2019/429/">LLDB: Beyond “po”</a></li>
<li><a href="https://objccn.io/issue-19-2/">与调试器共舞 - LLDB 的华尔兹</a></li>
<li><a href="https://developer.apple.com/videos/play/wwdc2018/412">Advanced Debugging with Xcode and LLDB</a></li>
<li><a href="https://juejin.cn/post/6844903504042000391">自定义LLDB命令实战</a></li>
<li><a href="https://muzico.gitee.io/niao/15704402390326.html#toc_40">LLDB基础</a></li>
<li><a href="https://developer.apple.com/library/archive/documentation/General/Conceptual/lldb-guide/chapters/Introduction.html#//apple_ref/doc/uid/TP40016717-CH1-DontLinkElementID_42">LLDB Debugging Guide</a></li>
<li><a href="https://developer.apple.com/library/archive/documentation/General/Conceptual/lldb-guide/chapters/Introduction.html#//apple_ref/doc/uid/TP40016717-CH1-DontLinkElementID_42">LLDB Debugging Guide</a></li>
</ol>
]]></content>
      <categories>
        <category>基础</category>
        <category>调试</category>
      </categories>
      <tags>
        <tag>swift</tag>
        <tag>OC</tag>
        <tag>调试</tag>
        <tag>Xcode</tag>
        <tag>LLDB</tag>
      </tags>
  </entry>
  <entry>
    <title>我就站在你面前，看我几分像从前</title>
    <url>/2022/12/09/%E6%88%91%E5%B0%B1%E7%AB%99%E5%9C%A8%E4%BD%A0%E9%9D%A2%E5%89%8D%EF%BC%8C%E7%9C%8B%E6%88%91%E5%87%A0%E5%88%86%E5%83%8F%E4%BB%8E%E5%89%8D/</url>
    <content><![CDATA[<h1 id="一、锲子"><a href="#一、锲子" class="headerlink" title="一、锲子"></a>一、锲子</h1><p>最近，抖音流行一句话“我就站在你面前，你看我几分像从前”；意思说我再也不是卑微的我了，你应该换个角度仔细审视我。这给人以苏秦携六国相印荣归故里的感觉。<br>笔者以前是做C++开发的，对C++语言比较熟悉，后来转做iOS开发，此后一直使用OC作为主要的开发语言。在做iOS开发期间，对Swift也了解过，然而接触不多，也没有多少实际开发经验。在没有仔细研究Swift之前，感觉其与OC也很类似，甚至说与其他现代的、工业级的编程语言相比，也都很类似。<br>近期，笔者抽空对Swift语法进行仔细的了解，发现Swift的进化是相当快速的，应该以一种全新的眼光仔细审视Swift，为迎接基于Swift的后浪做好准备。</p>
<span id="more"></span>

<h1 id="二、如何看待Swift"><a href="#二、如何看待Swift" class="headerlink" title="二、如何看待Swift"></a>二、如何看待Swift</h1><p>Swift业已推出多年，从2019起其基础语法已经趋于稳定，此时是深入研究与使用Swift的最后时机，如果再拖延就落后了。按照中国人的思维习惯，对一件事情一般要先进行定性，然后再进行研究。我们也依据此传统习惯，对Swift进行定性。<br>笔者认为：$\color{red}{Swift是一种全新的编程语言，而不能简单的认为它仅仅是除OC外的另一种选择。}$<br>原因有以下几点：</p>
<ol>
<li>Swift是安全的<br>广大读者或多或少的接触过Swift，也肯定听说过Swift相对OC来说更加安全。Swift确实是安全的语言，其提供许许多多的机制来实现更加安全的目标：</li>
</ol>
<ul>
<li>去除C、OC中的不安全做法（如隐式类型转换，switch语句case的自动穿透、case的不全覆盖、溢出的处理等等）</li>
<li>提供新的安全类型（如Optional）</li>
<li>严格的类型检查（在编译阶段就对类型进行检查，甚至有时候让你认为过于严格）</li>
</ul>
<ol start="2">
<li>Swift支持多种编程范式</li>
</ol>
<ul>
<li>面向过程编程</li>
<li>基于对象编程</li>
<li>面向对象编程</li>
<li>函数式编程</li>
<li>面向协议编程</li>
<li>泛型编程</li>
</ul>
<p>根据近期对Swift的学习、研究，发现其与C++11（笔者对C++14、C++20不熟悉）很类似。在C++98中就对泛型进行了良好的支持，在Swift中看到其对泛型的支持和C++98的泛型几乎如出一辙（当然在类型支持方面比C++98对泛型的支持更加强大），顿感亲切。<br>总之，笔者认为：<br>$\color{red}{Swift的表现力极其强大，给程序员的发挥空间更加广大！}$</p>
<h1 id="三、废多看码"><a href="#三、废多看码" class="headerlink" title="三、废多看码"></a>三、废多看码</h1><p>笔者不是搞理论研究的，也无法从理论角度总结出多么高深的理论来证明Swift多么好。那么就让我们废多看码（废话不要那么多了，上代码）吧！<br>接触过Swift的同学大多都了解：enum在Swift中的地位被提高，与Struct平级。下面我们就以代码+说明的形式，以enum为切入点，来展现Swift的表现力。本文先讲enum的基础语法，再讲基于enum的应用（想法有多远，就能走多远）。</p>
<h2 id="3-1-enum语法"><a href="#3-1-enum语法" class="headerlink" title="3.1 enum语法"></a>3.1 enum语法</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// swift的最基础基本用法</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MyEnumType</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">MyEnumTypeNone</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">MyEnumTypeFirst</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">MyEnumTypeSecond</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 具有基础值的枚举，与OC中的枚举差别也不大</span></span><br><span class="line"><span class="comment">// 枚举的基础值可以是数字、字符串</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MyEnumTypeWithBaseType</span>: <span class="title class_">Int</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">MyEnumTypeWithBaseTypeNone</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">MyEnumTypeWithBaseTypeFirst</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">MyEnumTypeWithBaseTypeSecond</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 文字型的基础值</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">House</span>: <span class="title class_">String</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">Baratheon</span> <span class="operator">=</span> <span class="string">&quot;Ours is the Fury&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Greyjoy</span> <span class="operator">=</span> <span class="string">&quot;We Do Not Sow&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Martell</span> <span class="operator">=</span> <span class="string">&quot;Unbowed, Unbent, Unbroken&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Stark</span> <span class="operator">=</span> <span class="string">&quot;Winter is Coming&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Tully</span> <span class="operator">=</span> <span class="string">&quot;Family, Duty, Honor&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Tyrell</span> <span class="operator">=</span> <span class="string">&quot;Growing Strong&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> bestHouse <span class="operator">=</span> <span class="type">House</span>.<span class="type">Stark</span></span><br><span class="line"><span class="built_in">print</span>(bestHouse.rawValue) <span class="comment">// prints &quot;Winter is coming&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以使用以下三种语法形式来处理枚举</span></span><br><span class="line"><span class="keyword">switch</span> aEnumType&#123;</span><br><span class="line">    <span class="keyword">case</span> .<span class="type">MyEnumTypeFirst</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;MyEnumTypeFirst&quot;</span>)</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">case</span> .<span class="type">MyEnumTypeFirst</span> <span class="operator">=</span> aEnumType&#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;MyEnumTypeFirst&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> aEnumType <span class="operator">==</span> .<span class="type">MyEnumTypeFirst</span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;MyEnumTypeFirst&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面我们看了枚举的最基础用法，看起来与C、OC中的也没有多大的那别。稍等，我们继续深入看。<br>$\color{red}{枚举除了可以指定基础值，还可以指定关联值！}$</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 我们来看看枚举关联值的具体语法</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> number(<span class="type">Int</span>)</span><br><span class="line">    <span class="keyword">case</span> message(<span class="type">String</span>)</span><br><span class="line">    <span class="keyword">case</span> fatal</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化一个具有关联值的枚举类型</span></span><br><span class="line"><span class="keyword">let</span> err : <span class="type">MyError</span> <span class="operator">=</span> .number(<span class="number">4</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化一个具有关联值的枚举类型</span></span><br><span class="line"><span class="keyword">let</span> num <span class="operator">=</span> <span class="number">4</span></span><br><span class="line"><span class="keyword">let</span> err : <span class="type">MyError</span> <span class="operator">=</span> .number(num)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果枚举的关联值有label，则调用的时候必须提供</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MyError2</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> number(<span class="type">Int</span>)</span><br><span class="line">    <span class="keyword">case</span> message(<span class="type">String</span>)</span><br><span class="line">    <span class="keyword">case</span> fatal(n:<span class="type">Int</span>, s:<span class="type">String</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> err : <span class="type">MyError2</span> <span class="operator">=</span> .fatal(n:<span class="operator">-</span><span class="number">12</span>, s:<span class="string">&quot;Oh the horror&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 默认枚举值是可以使用==号，进行判断是否相等的</span></span><br><span class="line"><span class="comment">// 但是如果一个枚举有关联值，则不能使用==</span></span><br><span class="line"><span class="keyword">if</span> err <span class="operator">==</span> <span class="type">MyError</span>.fatal &#123; <span class="comment">// 编译错误</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 但是如果此枚举实现了Equatable协议，则可以进行是否相同的判断</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MyError</span> : <span class="title class_">Equatable</span> &#123; <span class="comment">// *</span></span><br><span class="line">    <span class="keyword">case</span> number(<span class="type">Int</span>)</span><br></pre></td></tr></table></figure>
<p>让我们继续看枚举属性的语法与示例。</p>
<ul>
<li>枚举可以有实例属性、静态属性</li>
<li>枚举的属性只能是计算属性不能是存储属性</li>
<li>静态属性可以有属性包装器</li>
<li>实例属性不可有属性包装器<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Filter</span> : <span class="title class_">String</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> albums <span class="operator">=</span> <span class="string">&quot;Albums&quot;</span></span><br><span class="line">    <span class="keyword">case</span> playlists <span class="operator">=</span> <span class="string">&quot;Playlists&quot;</span></span><br><span class="line">    <span class="keyword">case</span> podcasts <span class="operator">=</span> <span class="string">&quot;Podcasts&quot;</span></span><br><span class="line">    <span class="keyword">case</span> books <span class="operator">=</span> <span class="string">&quot;Audiobooks&quot;</span></span><br><span class="line">    <span class="keyword">var</span> query : <span class="type">MPMediaQuery</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> <span class="keyword">self</span> &#123;</span><br><span class="line">        <span class="keyword">case</span> .albums:</span><br><span class="line">            <span class="keyword">return</span> .albums()</span><br><span class="line">        <span class="keyword">case</span> .playlists:</span><br><span class="line">            <span class="keyword">return</span> .playlists()</span><br><span class="line">        <span class="keyword">case</span> .podcasts:</span><br><span class="line">            <span class="keyword">return</span> .podcasts()</span><br><span class="line">        <span class="keyword">case</span> .books:</span><br><span class="line">            <span class="keyword">return</span> .audiobooks()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Silly</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> one</span><br><span class="line">    <span class="keyword">var</span> sillyProperty : <span class="type">String</span> &#123;</span><br><span class="line">        <span class="keyword">get</span> &#123; <span class="string">&quot;Howdy&quot;</span> &#125;</span><br><span class="line">        <span class="keyword">set</span> &#123;&#125; <span class="comment">// do nothing</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> silly <span class="operator">=</span> <span class="type">Silly</span>.one</span><br><span class="line">silly.sillyProperty <span class="operator">=</span> <span class="string">&quot;silly&quot;</span></span><br></pre></td></tr></table></figure>
让我们继续看枚举方法的语法与示例。</li>
<li>可以有实例方法、静态方法</li>
<li>修改枚举值的方法必须被表示为mutating</li>
<li>下标运算被默认为是会修改枚举值的，所以不需要标记为mutating<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> rectangle</span><br><span class="line">    <span class="keyword">case</span> ellipse</span><br><span class="line">    <span class="keyword">case</span> diamond</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">addShape</span> (<span class="params">to</span> <span class="params">p</span>: <span class="type">CGMutablePath</span>, <span class="params">in</span> <span class="params">r</span>: <span class="type">CGRect</span>) -&gt; () &#123;</span><br><span class="line">        <span class="keyword">switch</span> <span class="keyword">self</span> &#123;</span><br><span class="line">        <span class="keyword">case</span> .rectangle:</span><br><span class="line">            p.addRect(r)</span><br><span class="line">        <span class="keyword">case</span> .ellipse:</span><br><span class="line">            p.addEllipse(in:r)</span><br><span class="line">        <span class="keyword">case</span> .diamond:</span><br><span class="line">            p.move(to: <span class="type">CGPoint</span>(x:r.minX, y:r.midY))</span><br><span class="line">            p.addLine(to: <span class="type">CGPoint</span>(x: r.midX, y: r.minY))</span><br><span class="line">            p.addLine(to: <span class="type">CGPoint</span>(x: r.maxX, y: r.midY))</span><br><span class="line">            p.addLine(to: <span class="type">CGPoint</span>(x: r.midX, y: r.maxY))</span><br><span class="line">            p.closeSubpath()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 此枚举即指定了基础类型也指定了继承的协议</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Filter</span> : <span class="title class_">String</span>, <span class="title class_">CaseIterable</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> albums <span class="operator">=</span> <span class="string">&quot;Albums&quot;</span></span><br><span class="line">    <span class="keyword">case</span> playlists <span class="operator">=</span> <span class="string">&quot;Playlists&quot;</span></span><br><span class="line">    <span class="keyword">case</span> podcasts <span class="operator">=</span> <span class="string">&quot;Podcasts&quot;</span></span><br><span class="line">    <span class="keyword">case</span> books <span class="operator">=</span> <span class="string">&quot;Audiobooks&quot;</span></span><br><span class="line">    <span class="keyword">mutating</span> <span class="keyword">func</span> <span class="title function_">advance</span>() &#123;</span><br><span class="line">        <span class="keyword">let</span> cases <span class="operator">=</span> <span class="type">Filter</span>.allCases</span><br><span class="line">        <span class="keyword">var</span> ix <span class="operator">=</span> cases.firstIndex(of:<span class="keyword">self</span>)<span class="operator">!</span></span><br><span class="line">        ix <span class="operator">=</span> (ix <span class="operator">+</span> <span class="number">1</span>) <span class="operator">%</span> cases.count</span><br><span class="line">        <span class="keyword">self</span> <span class="operator">=</span> cases[ix]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> type <span class="operator">=</span> <span class="type">Filter</span>.books</span><br><span class="line">type.advance() <span class="comment">// type is now Filter.albums</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意<br>限于篇幅与本文的主题，以上不是枚举语法的全部；以上语法可以在5.1的版本编译，在其他版本上有待验证。</p>
</blockquote>
</li>
</ul>
<h2 id="3-2-该你了"><a href="#3-2-该你了" class="headerlink" title="3.2 该你了"></a>3.2 该你了</h2><p>$\color{red}{你能基于enum的语法创造出什么？}$<br>$\color{red}{你能基于enum的语法创造出什么？}$<br>$\color{red}{你能基于enum的语法创造出什么？}$</p>
<h2 id="3-3-Show-Time"><a href="#3-3-Show-Time" class="headerlink" title="3.3 Show Time"></a>3.3 Show Time</h2><p>在我看到以上语法的时候，感觉怎么这么复杂，特别是具有关联类型的枚举。心里在想，这能有什么用？！我想，此时大部分读者应该都与笔者有类似的想法，但是当笔者看到大牛们的作品时，震惊了。</p>
<h3 id="3-3-1-Optional"><a href="#3-3-1-Optional" class="headerlink" title="3.3.1 Optional"></a>3.3.1 Optional</h3><p>大部分读者对于Swift的Optional都听说过、使用过。但是，大家有看过他的实现吗？它有两大支柱：具有关联值的枚举、泛型（限于本文的主题，不会展开讲解泛型）。相信，任何介绍Swift的书籍、资料、文章都会几乎都会涉及到Optional。可以说它是Swift提供的非常重要的一种类型、机制，下面我们就通过Optional的部分源码来进行深入的学习。<br>让我们简单看一下Optional的语法。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 使用语法糖的Optional</span><br><span class="line">let shortForm: Int? = Int(&quot;42&quot;)</span><br><span class="line">// 标准的Optional</span><br><span class="line">let longForm: Optional&lt;Int&gt; = Int(&quot;42&quot;)</span><br><span class="line"></span><br><span class="line">// Optional是含有some、none两个值的枚举</span><br><span class="line">let number: Int? = Optional.some(42)</span><br><span class="line">let noNumber: Int? = Optional.none</span><br><span class="line">print(noNumber == nil) // Prints &quot;true&quot;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 有以下字典</span><br><span class="line">let imagePaths = [&quot;star&quot;: &quot;/glyphs/star.png&quot;,</span><br><span class="line">                  &quot;portrait&quot;: &quot;/images/content/portrait.jpg&quot;,</span><br><span class="line">                  &quot;spacer&quot;: &quot;/images/shared/spacer.gif&quot;]</span><br><span class="line"></span><br><span class="line">// 通过键来获取对应值其实返回的是Optional&lt;String&gt;</span><br><span class="line">if let starPath = imagePaths[&quot;star&quot;] &#123;</span><br><span class="line">  print(&quot;The star image is at &#x27;\(starPath)&#x27;&quot;)</span><br><span class="line">&#125; else &#123;</span><br><span class="line">  print(&quot;Couldn&#x27;t find the star image&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在回顾Optional的基本用法之后，让我们来看看它的源码。</p>
<blockquote>
<p>注意<br>以下代码有删减</p>
</blockquote>
<ul>
<li><p>基础代码</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">Optional</span>&lt;<span class="title class_">Wrapped</span>&gt; : <span class="title class_">ExpressibleByNilLiteral</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> none</span><br><span class="line">    <span class="keyword">case</span> <span class="keyword">some</span>(<span class="type">Wrapped</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Optional代表两类值：nil、某种类型的值</span></span><br><span class="line"><span class="comment">// none在这里代表nil</span></span><br><span class="line"><span class="comment">// some(wrapped) 代表某类型的值</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Optional&lt;Wrapped&gt;是个泛型定义，非本文重点，目前只需要知道这个代表将来的某种类型即可</span></span><br><span class="line"><span class="comment">// let n: nOptional&lt;Int&gt;  </span></span><br><span class="line"><span class="comment">// let str: nOptional&lt;String&gt;</span></span><br><span class="line"><span class="comment">// 这里的Int、String会代替源码里的Wrapped</span></span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
<li><p>基于nil字面量构建Optional</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> n: <span class="type">Optional</span>&lt;<span class="type">Int</span>&gt; <span class="operator">=</span> <span class="literal">nil</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 以上代码以及其变形是经常用到的，但是其背后是如何支持的呢</span></span><br><span class="line"><span class="comment">// Optional 实现了ExpressibleByNilLiteral协议</span></span><br><span class="line"><span class="comment">// ExpressibleByNilLiteral协议定义了一个初始化方法</span></span><br><span class="line"><span class="comment">// 该方法实现了基于nil字面量构建none类型的Optional</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">protocol</span> <span class="title class_">ExpressibleByNilLiteral</span> &#123;</span><br><span class="line">    <span class="keyword">init</span>(<span class="params">nilLiteral</span>: ())</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Optional实现了这个初始化方法，在176行</span></span><br></pre></td></tr></table></figure></li>
<li><p>基于某类型的值构建Optional</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">init</span>(<span class="keyword">_</span> <span class="params">some</span>: <span class="type">Wrapped</span>)</span><br><span class="line"><span class="comment">// Optional通过此初始化方法来构建一个实例</span></span><br></pre></td></tr></table></figure></li>
<li><p>Optional的模式匹配</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stream: <span class="type">DataStream</span>? <span class="operator">=</span> <span class="literal">nil</span></span><br><span class="line"><span class="keyword">switch</span> stream &#123;</span><br><span class="line"><span class="keyword">case</span> <span class="literal">nil</span>:</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">&quot;No data stream is configured.&quot;</span>)</span><br><span class="line"><span class="keyword">case</span> <span class="keyword">let</span> x<span class="operator">?</span>:</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">&quot;The data stream has <span class="subst">\(x.availableBytes)</span> bytes available.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出是 &quot;No data stream is configured.&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 注意基于case的Optional的解析使用的是模式匹配技术</span></span><br><span class="line"><span class="comment">// 而不是==</span></span><br><span class="line"><span class="comment">// 模式匹配采用的是~=运算符</span></span><br><span class="line"><span class="comment">// 以下是Optional类对模式匹配的支持</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">~=</span> (<span class="params">lhs</span>: _OptionalNilComparisonType, <span class="params">rhs</span>: <span class="type">Wrapped</span>?) -&gt; <span class="type">Bool</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
</li>
<li><p>与nil的对比 &#x3D;&#x3D;，右侧操作数是nil</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stream: <span class="type">DataStream</span>? <span class="operator">=</span> <span class="literal">nil</span></span><br><span class="line"><span class="keyword">if</span> stream <span class="operator">==</span> <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="built_in">print</span>(<span class="string">&quot;No data stream is configured.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出&quot;No data stream is configured.&quot;</span></span><br><span class="line"><span class="comment">// 此时左侧的Optional与nil的对比是调用如下的运算符方法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">==</span> (<span class="params">lhs</span>: <span class="type">Wrapped</span>?, <span class="params">rhs</span>: _OptionalNilComparisonType) -&gt; <span class="type">Bool</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>Optional与nil的对比 !&#x3D;，右侧操作数是nil</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stream: <span class="type">DataStream</span>? <span class="operator">=</span> fetchDataStream()</span><br><span class="line"><span class="keyword">if</span> stream <span class="operator">!=</span> <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="built_in">print</span>(<span class="string">&quot;The data stream has been configured.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出 &quot;The data stream has been configured.&quot;</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">!=</span> (<span class="params">lhs</span>: <span class="type">Wrapped</span>?, <span class="params">rhs</span>: _OptionalNilComparisonType) -&gt; <span class="type">Bool</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>与nil的对比 &#x3D;&#x3D;，左侧操作数是nil</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stream: <span class="type">DataStream</span>? <span class="operator">=</span> <span class="literal">nil</span></span><br><span class="line"><span class="keyword">if</span> <span class="literal">nil</span> <span class="operator">==</span> stream &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;No data stream is configured.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出 &quot;No data stream is configured.&quot;</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">==</span> (<span class="params">lhs</span>: _OptionalNilComparisonType, <span class="params">rhs</span>: <span class="type">Wrapped</span>?) -&gt; <span class="type">Bool</span></span><br></pre></td></tr></table></figure></li>
<li><p>与nil的对比!&#x3D;，左侧操作数是nil</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stream: <span class="type">DataStream</span>? <span class="operator">=</span> fetchDataStream()</span><br><span class="line"><span class="keyword">if</span> <span class="literal">nil</span> <span class="operator">!=</span> stream &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;The data stream has been configured.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出 &quot;The data stream has been configured.&quot;</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">!=</span> (<span class="params">lhs</span>: _OptionalNilComparisonType, <span class="params">rhs</span>: <span class="type">Wrapped</span>?) -&gt; <span class="type">Bool</span></span><br></pre></td></tr></table></figure></li>
<li><p>调试支持</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extension</span> <span class="title class_">Optional</span> : <span class="title class_">CustomDebugStringConvertible</span> &#123;</span><br><span class="line">    <span class="comment">/// A textual representation of this instance, suitable for debugging.</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">var</span> debugDescription: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>内省支持</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extension</span> <span class="title class_">Optional</span> : <span class="title class_">CustomReflectable</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">var</span> customMirror: <span class="type">Mirror</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>两个Optional的对比</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extension</span> <span class="title class_">Optional</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">!=</span> (<span class="params">lhs</span>: <span class="type">Optional</span>&lt;<span class="type">Wrapped</span>&gt;, <span class="params">rhs</span>: <span class="type">Optional</span>&lt;<span class="type">Wrapped</span>&gt;) -&gt; <span class="type">Bool</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="3-3-2-Result"><a href="#3-3-2-Result" class="headerlink" title="3.3.2 Result"></a>3.3.2 Result</h3><p>让我们先来看看Result的一个实例。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假如有下面的网络请求</span></span><br><span class="line"><span class="keyword">let</span> request <span class="operator">=</span> <span class="type">URLRequest</span>(url: <span class="type">URL</span>(string: <span class="string">&quot;https://xxxx&quot;</span>)<span class="operator">!</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">doSomeNetworking</span>(<span class="params">completion</span>:<span class="keyword">@escaping</span> (<span class="type">Data</span>) -&gt; ()) &#123;</span><br><span class="line">    <span class="type">URLSession</span>.shared.dataTask(with: request) &#123;</span><br><span class="line">        data, response, error <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> error <span class="operator">!=</span> <span class="literal">nil</span> &#123;</span><br><span class="line">                <span class="comment">//处理数据data</span></span><br><span class="line">                completion(data)</span><br><span class="line">             &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">//处理错误error</span></span><br><span class="line">                <span class="keyword">throw</span> error </span><br><span class="line">            &#125;</span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 但是throw error会有编译错误，因此此时不是出在一个可抛出错误的环境中</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 也许可以把doSomeNetworking的参数进行修改</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">doSomeNetworking</span>(<span class="params">completion</span>:<span class="keyword">@escaping</span> (<span class="type">Data</span>?, <span class="type">Error</span>?) -&gt; ()) &#123;</span><br><span class="line">    <span class="type">URLSession</span>.shared.dataTask(with: request) &#123;</span><br><span class="line">        data, response, error <span class="keyword">in</span></span><br><span class="line">        completion(data, error)</span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 针对此种情况，在Swift5中有新的解决方案</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">doSomeNetworking</span>(<span class="params">completion</span>:<span class="keyword">@escaping</span> (<span class="type">Result</span>&lt;<span class="type">Data</span>,<span class="type">Error</span>&gt;) -&gt; ()) &#123;    <span class="type">URLSession</span>.shared.dataTask(with: request) &#123;</span><br><span class="line">        data, <span class="keyword">_</span>, err <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> data <span class="operator">=</span> data &#123;</span><br><span class="line">            completion(.success(data))</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> err <span class="operator">=</span> err &#123;</span><br><span class="line">            completion(.failure(err))</span><br><span class="line">    &#125;</span><br><span class="line">&#125;.resume()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过以上代码，我们看到通过Reslut类型可以处理有正确的返回值或者由错误代码的情况</span></span><br></pre></td></tr></table></figure>

<p>下面让我们来看看Result的源码，在源码面前了无秘密。</p>
<ul>
<li>Result的核心代码<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">Result</span>&lt;<span class="title class_">Success</span>, <span class="title class_">Failure</span>&gt; <span class="title class_">where</span> <span class="title class_">Failure</span> : <span class="title class_">Error</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/// A success, storing a `Success` value.</span></span><br><span class="line">    <span class="keyword">case</span> success(<span class="type">Success</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">/// A failure, storing a `Failure` value.</span></span><br><span class="line">    <span class="keyword">case</span> failure(<span class="type">Failure</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 同样Result是枚举，含有两个值success、failure</span></span><br><span class="line"><span class="comment">// 其中失败必须是符合Error协议</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="3-3-3-基于枚举的二叉树"><a href="#3-3-3-基于枚举的二叉树" class="headerlink" title="3.3.3 基于枚举的二叉树"></a>3.3.3 基于枚举的二叉树</h3><p>看了上面还举得不过瘾吧？那让我们再继续看一个吧！<br>我们都知道在swift中枚举是值类型，而值类型不能递归定义（就是自身的定义中含有自身），那么就不能用枚举定义一个二叉树（二叉树的节点需要递归定义）。<br>这涉及到Swift的枚举的新语法，示例如下：</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在此枚举中Folder中的再次使用到了枚举FileNode</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">FileNode</span> &#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="type">File</span>(name: <span class="type">String</span>)</span><br><span class="line">  <span class="keyword">indirect</span> <span class="keyword">case</span> <span class="type">Folder</span>(name: <span class="type">String</span>, files: [<span class="type">FileNode</span>])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>下面我们看看如何通过枚举定义一个二叉树。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 常规的，在Swift中定义一个二叉树的节点一般采用class类型</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyTree</span>&lt;<span class="title class_">Element</span>: <span class="title class_">Comparable</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">var</span> value: <span class="type">Element</span>?</span><br><span class="line">    <span class="keyword">var</span> left: <span class="type">MyTree</span>&lt;<span class="type">Element</span>&gt;?</span><br><span class="line">    <span class="keyword">var</span> right: <span class="type">MyTree</span>&lt;<span class="type">Element</span>&gt;?</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如何表示一棵空树？自然而然想到的是把左右子树、value设置为空</span></span><br><span class="line"><span class="comment">// 但是这不够好</span></span><br><span class="line"><span class="comment">// 看下面基于枚举的定义</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">indirect</span> <span class="keyword">enum</span> <span class="title class_">MyNewTree</span>&lt;<span class="title class_">Element</span>: <span class="title class_">Comparable</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="type">EmptyTree</span></span><br><span class="line">    <span class="keyword">case</span> <span class="type">Node</span>(left: <span class="type">MyNewTree</span>&lt;<span class="type">Element</span>&gt;, <span class="type">Element</span>, reight: <span class="type">MyNewTree</span>&lt;<span class="type">Element</span>&gt;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="四、总结"><a href="#四、总结" class="headerlink" title="四、总结"></a>四、总结</h1><p>通过展示基于枚举的巧妙运用，相信大家对Swift的表现力有更进一步的认识。虽然大多数语言都可以构建巧妙的运用，但是Swift的可能性更大。<br>笔者才疏学浅，也是最近才对Swift进行一些研究。虽然了解还很粗浅，但是确实被Swift所吸引，现在的Swift与C++很类似，但是在安全性方面更胜一筹。针对这种提供多种编程范式的语言，需要仔细研究，方可运用自如。</p>
]]></content>
      <categories>
        <category>swift</category>
      </categories>
      <tags>
        <tag>swift</tag>
        <tag>OC</tag>
      </tags>
  </entry>
  <entry>
    <title>Swift高阶函数解析</title>
    <url>/2022/12/09/Swift%E9%AB%98%E9%98%B6%E5%87%BD%E6%95%B0%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h1 id="一、锲子"><a href="#一、锲子" class="headerlink" title="一、锲子"></a>一、锲子</h1><p>最近在学习Swift过程中发现，Swift有不少高阶函数，这些函数为Swift支持函数式编程范式提供了强有力的支持。这些函数主要包括：map、filter、flatMap、compactMap、reduce等。<br>本文对这些函数进行解析，与网络上的其他文章相比，本文有两大特色：</p>
<ul>
<li>针对这些函数原型进行说明（如果不了解泛型，看到这些函数原型时，可能一脸懵逼；理解了这些原型，就更加理解函数本身）</li>
<li>reduce方法是很强大的，本文使用reduce方法重新实现其他的高阶函数</li>
<li>包含关于lazy用法的说明（非常规的属性lazy用法）</li>
</ul>
<blockquote>
<p>注意<br>本文的函数介绍以Array的为例，其他的十分类似。</p>
</blockquote>
<span id="more"></span>

<h1 id="二、高阶函数解析"><a href="#二、高阶函数解析" class="headerlink" title="二、高阶函数解析"></a>二、高阶函数解析</h1><h2 id="2-1-map"><a href="#2-1-map" class="headerlink" title="2.1 map"></a>2.1 map</h2><ul>
<li>作用<br>如果用一句话来概括map的作用，那么就是：以序列的每个元素为实参来调用用户提供的转换方法来获取一个新的结果，并用这个结果构成一个新的序列</li>
<li>函数原型<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@inlinable</span> <span class="keyword">public</span> <span class="keyword">func</span> <span class="title function_">map</span>&lt;<span class="type">T</span>&gt;(<span class="keyword">_</span> <span class="params">transform</span>: (<span class="type">Element</span>) <span class="keyword">throws</span> -&gt; <span class="type">T</span>) <span class="keyword">rethrows</span> -&gt; [<span class="type">T</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">// @inlinable 这是在SE-0193中说明的属性，用于告知编译器可以为泛型的具体类型替换生成内联代码，至于是否最终内联，是由编译器决定的</span></span><br><span class="line"><span class="comment">// public 可以被其他模块看到该方法</span></span><br><span class="line"><span class="comment">// func 声明一个函数的关键字</span></span><br><span class="line"><span class="comment">// map 函数名</span></span><br><span class="line"><span class="comment">// &lt;T&gt; 这是泛型函数声明泛型类型占位符的方式，在显式、隐式特化时，会被替换为具体的类型</span></span><br><span class="line"><span class="comment">// _ 标识函数参数的外部参数名被忽略</span></span><br><span class="line"><span class="comment">// transform 是一个参数名</span></span><br><span class="line"><span class="comment">// (Element) throws -&gt; T 这里标识transform的类型，该类型是一个函数，该函数有一个形参（形参的类型是序列的类型，Element），该函数可能抛出错误（throws），该函数的返回值是T</span></span><br><span class="line"><span class="comment">// rethrows 标识，如果用户提供的错误抛出异常，则会再次抛出给用户</span></span><br><span class="line"><span class="comment">// [T]标识最后返回的是一个数组，数组的类型是T</span></span><br></pre></td></tr></table></figure></li>
<li>示例<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是一个很普通的例子，仅用于说明map的最基础功能</span></span><br><span class="line"><span class="keyword">let</span> arrInt <span class="operator">=</span> [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrInt.map &#123; (aElement) -&gt; <span class="type">String</span> <span class="keyword">in</span></span><br><span class="line">    <span class="type">String</span>(aElement)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [&quot;0&quot;, &quot;1&quot;, &quot;2&quot;, &quot;3&quot;, &quot;4&quot;]</span></span><br></pre></td></tr></table></figure>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 此示例用于说明在可能碰到nil值时的处理方式</span></span><br><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;3&quot;</span>, <span class="string">&quot;4&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.map &#123; (aElement) -&gt; <span class="type">Int</span>? <span class="keyword">in</span></span><br><span class="line">    <span class="type">Int</span>(aElement)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">//[Optional(0), Optional(1), Optional(2), Optional(3), Optional(4), nil]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 由于&quot;wwj&quot;无法转换为数字，所以笔者提供的转换方法返回了一个Int？</span></span><br><span class="line"><span class="comment">// 这导致最终的arrNew的元素类型是Int?</span></span><br><span class="line"><span class="comment">// 这在某些情况下不方便，所以就请我们看看compactMap是如何处理可选值的</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-2-compactMap"><a href="#2-2-compactMap" class="headerlink" title="2.2 compactMap"></a>2.2 compactMap</h2><ul>
<li>作用<br>compactMap的作用于map类似，差别是针对nil值的处理方式不同，compactMap把nil从最终结果过滤出去。</li>
<li>函数原型<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@inlinable</span> <span class="keyword">public</span> <span class="keyword">func</span> <span class="title function_">compactMap</span>&lt;<span class="type">ElementOfResult</span>&gt;(<span class="keyword">_</span> <span class="params">transform</span>: (<span class="type">Element</span>) <span class="keyword">throws</span> -&gt; <span class="type">ElementOfResult</span>?) <span class="keyword">rethrows</span> -&gt; [<span class="type">ElementOfResult</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">// 此函数原型没有新的内容，所以不再做介绍</span></span><br></pre></td></tr></table></figure></li>
<li>示例<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;3&quot;</span>, <span class="string">&quot;4&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.compactMap &#123; (aElement) -&gt; <span class="type">Int</span>? <span class="keyword">in</span></span><br><span class="line">    <span class="type">Int</span>(aElement)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [0, 1, 2, 3, 4]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以看出，最终结果arrNew元素的类型是Int，且不包含nil</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 以上方法可以替换为下面的方法</span></span><br><span class="line"><span class="comment">// 差别在笔者提供的类型转换方法当中</span></span><br><span class="line"><span class="comment">// 如果对这里不懂或者想了解更多内容请参阅笔者的《Chapter 2.Functions》</span></span><br><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;3&quot;</span>, <span class="string">&quot;4&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.compactMap &#123;<span class="type">Int</span>(<span class="variable">$0</span>)&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [0, 1, 2, 3, 4]</span></span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-3-flatMap"><a href="#2-3-flatMap" class="headerlink" title="2.3 flatMap"></a>2.3 flatMap</h2><ul>
<li>作用<br>flatMap的作用于map类似，差别是此处用户提供的转换方法返回值是一个数组、最终返回的数组是把用户返回的数组拉平了。</li>
<li>函数原型<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@inlinable</span> <span class="keyword">public</span> <span class="keyword">func</span> <span class="title function_">flatMap</span>&lt;<span class="type">SegmentOfResult</span>&gt;(<span class="keyword">_</span> <span class="params">transform</span>: (<span class="type">Element</span>) <span class="keyword">throws</span> -&gt; <span class="type">SegmentOfResult</span>) <span class="keyword">rethrows</span> -&gt; [<span class="type">SegmentOfResult</span>.<span class="type">Element</span>] <span class="keyword">where</span> <span class="type">SegmentOfResult</span> : <span class="type">Sequence</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里只针对与map原型相比，不同的地方进行介绍</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// [SegmentOfResult.Element] 与map相比，此处返回的是数组</span></span><br><span class="line"><span class="comment">// where 在此处是对泛型的类型进行约束</span></span><br><span class="line"><span class="comment">// SegmentOfResult : Sequence 返回值的类型占位符是SegmentOfResult，此处要求返回数组的元素类型必须符合Sequence协议</span></span><br></pre></td></tr></table></figure></li>
<li>示例<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;12&quot;</span>, <span class="string">&quot;32&quot;</span>, <span class="string">&quot;34&quot;</span>, <span class="string">&quot;54&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.flatMap &#123; (aElement) -&gt; [<span class="type">Character</span>] <span class="keyword">in</span></span><br><span class="line">    aElement.shuffled()</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [&quot;0&quot;, &quot;2&quot;, &quot;1&quot;, &quot;3&quot;, &quot;2&quot;, &quot;3&quot;, &quot;4&quot;, &quot;4&quot;, &quot;5&quot;, &quot;w&quot;, &quot;w&quot;, &quot;j&quot;]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 字符串其实也是符合序列协议的类型</span></span><br><span class="line"><span class="comment">// 字符串这种序列的元素类型是Character</span></span><br><span class="line"><span class="comment">// shuffled 方法是把组成字符串的字符打乱顺序后以数组的形式返回所有的字符</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-4-filter"><a href="#2-4-filter" class="headerlink" title="2.4 filter"></a>2.4 filter</h2><ul>
<li>作用<br>flatMap的作用于map类似，差别是用户提供的函数作为一个过滤网，通过网的元素组成新的数组，并返回该数组。</li>
<li>函数原型<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@inlinable</span> <span class="keyword">public</span> <span class="keyword">func</span> <span class="title function_">filter</span>(<span class="keyword">_</span> <span class="params">isIncluded</span>: (<span class="type">String</span>) <span class="keyword">throws</span> -&gt; <span class="type">Bool</span>) <span class="keyword">rethrows</span> -&gt; [<span class="type">String</span>]</span><br><span class="line"><span class="comment">// 此原型没有新内容，故不作解释</span></span><br></pre></td></tr></table></figure></li>
<li>示例<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;12&quot;</span>, <span class="string">&quot;32&quot;</span>, <span class="string">&quot;34&quot;</span>, <span class="string">&quot;54&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.filter &#123; (aElement) -&gt; <span class="type">Bool</span> <span class="keyword">in</span></span><br><span class="line">    aElement.contains(<span class="string">&quot;2&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [&quot;12&quot;, &quot;32&quot;]</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-5-reduce"><a href="#2-5-reduce" class="headerlink" title="2.5 reduce"></a>2.5 reduce</h2><p>reduce是一个重载函数，其具有两种类型，下面分别做介绍。</p>
<h3 id="2-5-1-第一种reduce"><a href="#2-5-1-第一种reduce" class="headerlink" title="2.5.1 第一种reduce"></a>2.5.1 第一种reduce</h3><ul>
<li>作用<br>使用用户提供的函数对序列的元素进行结合。如果是第一次看到的话，不好理解，一会看代码就理解了。</li>
<li>函数原型<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@inlinable</span> <span class="keyword">public</span> <span class="keyword">func</span> <span class="title function_">reduce</span>&lt;<span class="type">Result</span>&gt;(<span class="keyword">_</span> <span class="params">initialResult</span>: <span class="type">Result</span>, <span class="keyword">_</span> <span class="params">nextPartialResult</span>: (<span class="type">Result</span>, <span class="type">Element</span>) <span class="keyword">throws</span> -&gt; <span class="type">Result</span>) <span class="keyword">rethrows</span> -&gt; <span class="type">Result</span></span><br><span class="line"><span class="comment">// 此原型没有新内容，故不作解释</span></span><br></pre></td></tr></table></figure></li>
<li>示例<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;12&quot;</span>, <span class="string">&quot;32&quot;</span>, <span class="string">&quot;34&quot;</span>, <span class="string">&quot;54&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.reduce(<span class="string">&quot;&quot;</span>) &#123; (acc, aElement) -&gt; <span class="type">String</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">return</span> acc <span class="operator">+</span> aElement</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// 012323454wwj</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个例子简单说明了reduce的用法，很多介绍的文章也只说到这里</span></span><br><span class="line"><span class="comment">// 此函数的计算过程是这样子的</span></span><br><span class="line"><span class="comment">// 使用初始值，作为实参调用用户的函数，此时得到一个返回值</span></span><br><span class="line"><span class="comment">// 然后以新的返回值作为实参，再次调用用户的函数</span></span><br><span class="line"><span class="comment">// 重复上面步骤，指导序列结束</span></span><br><span class="line"><span class="comment">// 把用户函数最后一次调用的返回值，作为reduce的返回值，返回给用户</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="2-5-2-第二种reduce"><a href="#2-5-2-第二种reduce" class="headerlink" title="2.5.2 第二种reduce"></a>2.5.2 第二种reduce</h3><ul>
<li>作用<br>使用用户提供的函数对序列的元素进行结合。如果是第一次看到的话，不好理解，一会看代码就理解了。</li>
<li>函数原型<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@inlinable</span> <span class="keyword">public</span> <span class="keyword">func</span> <span class="title function_">reduce</span>&lt;<span class="type">Result</span>&gt;(<span class="params">into</span> <span class="params">initialResult</span>: <span class="type">Result</span>, <span class="keyword">_</span> <span class="params">updateAccumulatingResult</span>: (<span class="keyword">inout</span> <span class="type">Result</span>, <span class="type">Element</span>) <span class="keyword">throws</span> -&gt; ()) <span class="keyword">rethrows</span> -&gt; <span class="type">Result</span></span><br><span class="line"><span class="comment">// inout 这里的Result的类型是一个可改变的类型，这很重要</span></span><br></pre></td></tr></table></figure></li>
<li>示例<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;12&quot;</span>, <span class="string">&quot;32&quot;</span>, <span class="string">&quot;34&quot;</span>, <span class="string">&quot;54&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> strNew <span class="operator">=</span> arrString.reduce(into: <span class="string">&quot;&quot;</span>) &#123; (acc, aElement) <span class="keyword">in</span></span><br><span class="line">    acc.append(aElement) <span class="comment">// （1）请注意这里</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(strNew) <span class="comment">// 012323454wwj</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个方法的返回值与上面一个相同，但是请注意（1）这一行，他是通过修改acc来实现的，也就是说acc是一个输入输出参数</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="三、以reduce实现其他高阶函数"><a href="#三、以reduce实现其他高阶函数" class="headerlink" title="三、以reduce实现其他高阶函数"></a>三、以reduce实现其他高阶函数</h1><p>大部分网文在讲述的时候，都只涉及以上内容，但不包括第二种reduce函数。其实，其作用非常强大，下面我们就用其来实现其他几个高阶函数。</p>
<h2 id="3-1-reduce实现map"><a href="#3-1-reduce实现map" class="headerlink" title="3.1 reduce实现map"></a>3.1 reduce实现map</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrInt <span class="operator">=</span> [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrInt.reduce(into: [<span class="type">String</span>]()) &#123; (acc, aElement) <span class="keyword">in</span></span><br><span class="line">    acc.append(<span class="type">String</span>(aElement))</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [&quot;0&quot;, &quot;1&quot;, &quot;2&quot;, &quot;3&quot;, &quot;4&quot;]</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="3-2-reduce实现compactMap"><a href="#3-2-reduce实现compactMap" class="headerlink" title="3.2 reduce实现compactMap"></a>3.2 reduce实现compactMap</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;3&quot;</span>, <span class="string">&quot;4&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.reduce(into: [<span class="type">Int</span>]()) &#123; (acc, aElement) <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> aStr <span class="operator">=</span> <span class="type">Int</span>(aElement) &#123;</span><br><span class="line">        acc.append(aStr)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [0, 1, 2, 3, 4]</span></span><br></pre></td></tr></table></figure>
<h2 id="3-3-reduce实现flatMap"><a href="#3-3-reduce实现flatMap" class="headerlink" title="3.3 reduce实现flatMap"></a>3.3 reduce实现flatMap</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;12&quot;</span>, <span class="string">&quot;32&quot;</span>, <span class="string">&quot;34&quot;</span>, <span class="string">&quot;54&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.reduce(into: [<span class="type">Character</span>](), &#123; (acc, aElement) <span class="keyword">in</span></span><br><span class="line">    acc.append(contentsOf: aElement.shuffled())</span><br><span class="line">&#125;)</span><br><span class="line">    </span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [&quot;0&quot;, &quot;2&quot;, &quot;1&quot;, &quot;3&quot;, &quot;2&quot;, &quot;3&quot;, &quot;4&quot;, &quot;4&quot;, &quot;5&quot;, &quot;w&quot;, &quot;w&quot;, &quot;j&quot;]</span></span><br></pre></td></tr></table></figure>
<h2 id="3-3-reduce实现filter"><a href="#3-3-reduce实现filter" class="headerlink" title="3.3 reduce实现filter"></a>3.3 reduce实现filter</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> [<span class="string">&quot;0&quot;</span>, <span class="string">&quot;12&quot;</span>, <span class="string">&quot;32&quot;</span>, <span class="string">&quot;34&quot;</span>, <span class="string">&quot;54&quot;</span>, <span class="string">&quot;wwj&quot;</span>]</span><br><span class="line"><span class="keyword">let</span> arrNew <span class="operator">=</span> arrString.reduce(into: [<span class="type">String</span>]()) &#123; (acc, aElement) <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> aElement.contains(<span class="string">&quot;2&quot;</span>) &#123;</span><br><span class="line">        acc.append(aElement)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(arrNew) <span class="comment">// [&quot;12&quot;, &quot;32&quot;]</span></span><br></pre></td></tr></table></figure>
<h1 id="四、reduce的妙用"><a href="#四、reduce的妙用" class="headerlink" title="四、reduce的妙用"></a>四、reduce的妙用</h1><p>通过第三节，可以看出来，reduce十分强大，可以实现其他高阶函数。其实，reduce的强大超过你的想象。一些看起来不可能用reduce实现的功能，都可以用reduce实现。下面，我们看一些示例。</p>
<h2 id="4-1-根据考分进行分级"><a href="#4-1-根据考分进行分级" class="headerlink" title="4.1 根据考分进行分级"></a>4.1 根据考分进行分级</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> name: <span class="type">String</span></span><br><span class="line">    <span class="keyword">let</span> scroeTotale: <span class="type">Int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> allStudent <span class="operator">=</span> [</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;wwj&quot;</span>, scroeTotale: <span class="number">12</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;wzy&quot;</span>, scroeTotale: <span class="number">21</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;sll&quot;</span>, scroeTotale: <span class="number">19</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;sr&quot;</span>, scroeTotale: <span class="number">1</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;sxx&quot;</span>, scroeTotale: <span class="operator">-</span><span class="number">1</span>)</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> studentGrade <span class="operator">=</span> allStudent.reduce(into: [String: [<span class="type">Student</span>]]()) &#123; ( acc, aStudent) <span class="keyword">in</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> grade: <span class="type">String</span></span><br><span class="line">    <span class="keyword">switch</span> aStudent.scroeTotale &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="operator">...</span><span class="number">0</span>:</span><br><span class="line">        grade <span class="operator">=</span> <span class="string">&quot;差等生&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="number">1</span><span class="operator">...</span><span class="number">10</span>:</span><br><span class="line">        grade <span class="operator">=</span> <span class="string">&quot;低等生&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="number">11</span><span class="operator">...</span><span class="number">20</span>:</span><br><span class="line">        grade <span class="operator">=</span> <span class="string">&quot;中等生&quot;</span></span><br><span class="line">    <span class="keyword">case</span> <span class="number">21</span><span class="operator">...</span>:</span><br><span class="line">        grade <span class="operator">=</span> <span class="string">&quot;优等生&quot;</span></span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        grade <span class="operator">=</span> <span class="string">&quot;编译器导致此处必须包含default子句，不然会报错误：switch是不完整的&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> <span class="keyword">_</span> <span class="operator">=</span> acc[grade] &#123;</span><br><span class="line">        acc[grade]<span class="operator">?</span>.append(aStudent)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        acc[grade] <span class="operator">=</span> [aStudent]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">[&quot;低等生&quot;: [myLearnSwift.Student(name: &quot;sr&quot;, scroeTotale: 1)], </span></span><br><span class="line"><span class="comment">&quot;优等生&quot;: [myLearnSwift.Student(name: &quot;wzy&quot;, scroeTotale: 21)],</span></span><br><span class="line"><span class="comment">&quot;差等生&quot;: [myLearnSwift.Student(name: &quot;sxx&quot;, scroeTotale: -1)],</span></span><br><span class="line"><span class="comment">&quot;中等生&quot;: [myLearnSwift.Student(name: &quot;wwj&quot;, scroeTotale: 12), myLearnSwift.Student(name: &quot;sll&quot;, scroeTotale: 19)]]</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>
<h2 id="4-2-获得优等生的人数和平均分"><a href="#4-2-获得优等生的人数和平均分" class="headerlink" title="4.2 获得优等生的人数和平均分"></a>4.2 获得优等生的人数和平均分</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> name: <span class="type">String</span></span><br><span class="line">    <span class="keyword">let</span> scroeTotale: <span class="type">Int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> allStudent <span class="operator">=</span> [</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;wwj&quot;</span>, scroeTotale: <span class="number">12</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;wzy&quot;</span>, scroeTotale: <span class="number">21</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;sll&quot;</span>, scroeTotale: <span class="number">19</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;sr&quot;</span>, scroeTotale: <span class="number">1</span>),</span><br><span class="line">    <span class="type">Student</span>(name: <span class="string">&quot;sxx&quot;</span>, scroeTotale: <span class="operator">-</span><span class="number">1</span>)</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> studentGrade <span class="operator">=</span> allStudent.reduce(into: (number: <span class="number">0</span>, average: <span class="number">0</span>)) &#123; ( acc, aStudent) <span class="keyword">in</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> aStudent.scroeTotale <span class="operator">&gt;</span> <span class="number">10</span> &#123;</span><br><span class="line">        acc.average <span class="operator">=</span> (acc.number <span class="operator">*</span> acc.average <span class="operator">+</span> aStudent.scroeTotale) <span class="operator">/</span> (acc.number <span class="operator">+</span> <span class="number">1</span>)</span><br><span class="line">        acc.number <span class="operator">+=</span> <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(studentGrade) <span class="comment">// (number: 3, average: 17)</span></span><br></pre></td></tr></table></figure>
<h1 id="五、lazy"><a href="#五、lazy" class="headerlink" title="五、lazy"></a>五、lazy</h1><h2 id="5-1-lazy示例"><a href="#5-1-lazy示例" class="headerlink" title="5.1 lazy示例"></a>5.1 lazy示例</h2><p>常规的lazy一般用来标识某属性，该属性在真正用到的时候才会被构建，而不是其所属对象生成时就需要构造。通过lazy关键字，最主要的是提升了性能。除此之外，在以上的高阶方法中也可以使用lazy。高阶函数的lazy作用与之前提到的相同，主要是为了提高性能。下面就让我们看看lazy的具体用法吧。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arrInt <span class="operator">=</span> [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]</span><br><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> arrInt.map &#123; (aElement) -&gt; <span class="type">String</span> <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(aElement)</span><br><span class="line">    <span class="keyword">return</span> <span class="type">String</span>(aElement)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">输出结果是：</span></span><br><span class="line"><span class="comment">1</span></span><br><span class="line"><span class="comment">2</span></span><br><span class="line"><span class="comment">3</span></span><br><span class="line"><span class="comment">4</span></span><br><span class="line"><span class="comment">5</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过以上map的示例，可以看出，map一定会执行</span></span><br><span class="line"><span class="comment">// 但是，存在一些情况：此时你不想、不能、不愿，立刻、马上获得新的数据</span></span><br><span class="line"><span class="comment">// 获取map方法过于耗费性能、或许是因为没有必要对所有元素执行map等等原因</span></span><br><span class="line"><span class="comment">// 针对此问题，可以通过lazy来解决</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> arrInt <span class="operator">=</span> [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]</span><br><span class="line"><span class="keyword">let</span> arrString <span class="operator">=</span> arrInt.lazy.map &#123; (aElement) -&gt; <span class="type">String</span> <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(aElement)</span><br><span class="line">    <span class="keyword">return</span> <span class="type">String</span>(aElement)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 执行以上语句后，没有任何输出，这说明：实际的map闭包没有被执行</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(arrString[<span class="number">0</span>])</span><br><span class="line"><span class="built_in">print</span>(arrString[<span class="number">1</span>])</span><br><span class="line"><span class="built_in">print</span>(arrString[<span class="number">0</span>])</span><br><span class="line"><span class="comment">// 执行以上语句之后，输出内容如下</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">1 map输出</span></span><br><span class="line"><span class="comment">1 print输出</span></span><br><span class="line"><span class="comment">2 map输出</span></span><br><span class="line"><span class="comment">2 print输出</span></span><br><span class="line"><span class="comment">1 map输出</span></span><br><span class="line"><span class="comment">1 print输出</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>
<h2 id="5-2-lazy总结"><a href="#5-2-lazy总结" class="headerlink" title="5.2 lazy总结"></a>5.2 lazy总结</h2><p>通过以上示例，我们对lazy的用法等内容进行总结：</p>
<ul>
<li>用法<ul>
<li>在容器与高阶函数之间添加lazy</li>
<li>其他用法不变</li>
</ul>
</li>
<li>注意事项<ul>
<li>通过lazy属性的引入，高阶函数不是马上被执行</li>
<li>只有在真正需要的时候map函数才执行（如上例中print(arrString[0])）被调用时</li>
</ul>
</li>
<li>性能问题<ul>
<li>由于lazy没有缓存以及具体实现问题，所以其性能提升并不明显，有时甚至不如直接使用高阶函数的性能好</li>
<li>所以只有在原高阶函数确实性能影响较大的时候使用lazy性能才有较大提升</li>
</ul>
</li>
</ul>
<h1 id="六、转变思想"><a href="#六、转变思想" class="headerlink" title="六、转变思想"></a>六、转变思想</h1><p>由于Swift大大简化了for循环的写法，所以一般看来其与这些高阶函数差别不大。但是，虽然差别不大，这代表这不同的编程范式。我们要勇于接受新的函数式的编程范式，克服命令式编程范式的肌肉记忆。</p>
]]></content>
  </entry>
  <entry>
    <title>掌握Swift泛型：一个实际的代码重用示例</title>
    <url>/2022/12/09/%E6%8E%8C%E6%8F%A1Swift%E6%B3%9B%E5%9E%8B%EF%BC%9A%E4%B8%80%E4%B8%AA%E5%AE%9E%E9%99%85%E7%9A%84%E4%BB%A3%E7%A0%81%E9%87%8D%E7%94%A8%E7%A4%BA%E4%BE%8B/</url>
    <content><![CDATA[<h1 id="一、序言"><a href="#一、序言" class="headerlink" title="一、序言"></a>一、序言</h1><p>泛型是Swift的一项强大功能，可让您以其他方式无法实现的方式泛化和重用代码。泛型具有许多强大的特型，这些特型也成为了许多开发人员的障碍。iOS的SDK广泛的使用了泛型，特别是在SwiftUI中。在这篇文章中，我讲解释为什么泛型存在并且如何在你的App里使用它。</p>
<span id="more"></span>

<h1 id="二、直到遇到真实的泛型使用场景之后你才能真正理解泛型"><a href="#二、直到遇到真实的泛型使用场景之后你才能真正理解泛型" class="headerlink" title="二、直到遇到真实的泛型使用场景之后你才能真正理解泛型"></a>二、直到遇到真实的泛型使用场景之后你才能真正理解泛型</h1><p>Swift的标准库与其他的苹果的库中都大量使用了泛型。<br>幸运的是，Swift编译器执行的类型推断通常将泛型代码隐藏在熟悉的代码后面。 你可能在构建iOS应用程序时走得很远，而无需了解泛型或不知道它们的存在。<br>但是，在某些情况下，你与泛型终究是会相遇。<br>正如我再这篇文章里想你展示的那样——你无法重用你的代码，除非你掌握了Swift的泛型。不但如此，泛型是<a href="https://matteomanferdini.com/protocol-oriented-programming/">面向协议编程</a>的基础，特别是在为你的App构建<a href="https://matteomanferdini.com/network-requests-rest-apis-ios-swift/">坚固的网络层</a>时。<br>根据我的经验，我的学生经常很难理解Swift泛型。 问题在于，在没有了解因缺乏泛型而导致的代码局限之前，很难理解泛型为何有用。<br>你通常需要一些曾经作为开发人员的经验才能理解到这点。 最后，如果初学者甚至中级iOS开发人员接受代码中的某些重复内容，都可以在不使用泛型的情况下开发应用。<br>我看过许多文章，这些文章直接解释了什么泛型，并向你展示了如何从一开始就使用它们。 我在这种方法中发现的问题是，它仅教你泛型如何在该语言中工作。<br>你没有学习到的是——如何识别那种场景（在这种场景中使用泛型可以获取好处）。<br>所以，在这里，我讲采用相反的方法。我将从简单的、特殊的代码开始，直到需要泛型的时候再把泛型引入到代码里。</p>
<h1 id="三、大部分你写的代码都隐式的使用泛型，从而是你的生活简单一些"><a href="#三、大部分你写的代码都隐式的使用泛型，从而是你的生活简单一些" class="headerlink" title="三、大部分你写的代码都隐式的使用泛型，从而是你的生活简单一些"></a>三、大部分你写的代码都隐式的使用泛型，从而是你的生活简单一些</h1><p>举例来说，让我们为一个社交网络应用（类似Facebook或Meetup）构建几个界面，人们可以在其中加入活动。<a href="https://github.com/matteom/Events">查看源码请点我</a>。<img src="/2022/12/09/%E6%8E%8C%E6%8F%A1Swift%E6%B3%9B%E5%9E%8B%EF%BC%9A%E4%B8%80%E4%B8%AA%E5%AE%9E%E9%99%85%E7%9A%84%E4%BB%A3%E7%A0%81%E9%87%8D%E7%94%A8%E7%A4%BA%E4%BE%8B/preview1.png" class="" title="This is an image">正如你看到的，这两个界面很相似。它们展示不同的信息，但是它们的结构是相同的。<br>你创建的有意义的应用程序都将具有要出于不同目的重用的代码。<br>有时候，这很明显，就像上面的两个应用程序界面一样。 但这可能出现在代码的任何部分。<br>让我们开始定义一些可用于填充应用界面的数据。 此类数据通常来自网络的JSON数据，因此将其存储在Xcode项目中的.json文件中以进行测试是一种很好的做法。</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Events.json</span></span><br><span class="line"><span class="punctuation">[</span></span><br><span class="line">	<span class="punctuation">&#123;</span></span><br><span class="line">		<span class="attr">&quot;title&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Book club&quot;</span><span class="punctuation">,</span></span><br><span class="line">		<span class="attr">&quot;date&quot;</span><span class="punctuation">:</span> <span class="number">1591454840</span><span class="punctuation">,</span></span><br><span class="line">		<span class="attr">&quot;participants&quot;</span><span class="punctuation">:</span> <span class="number">12</span></span><br><span class="line">	<span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">	...</span><br><span class="line"><span class="punctuation">]</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// Participants.json</span></span><br><span class="line"><span class="punctuation">[</span></span><br><span class="line">	<span class="punctuation">&#123;</span></span><br><span class="line">		<span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Quinten Kortum&quot;</span><span class="punctuation">,</span></span><br><span class="line">		<span class="attr">&quot;friends&quot;</span><span class="punctuation">:</span> <span class="number">4</span><span class="punctuation">,</span></span><br><span class="line">		<span class="attr">&quot;joined&quot;</span><span class="punctuation">:</span> <span class="number">1578250800</span></span><br><span class="line">	<span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">	...</span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure>
<p>为简洁起见，上面我仅列出每个文件一项。你可以在Xcode项目中找到完整的数据。<br>现在，我们需要两种模型类型来表示应用程序中的事件和人物，并使用Codable协议解码JSON数据。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Event</span>: <span class="title class_">Decodable</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> title: <span class="type">String</span></span><br><span class="line">	<span class="keyword">let</span> date: <span class="type">Date</span></span><br><span class="line">	<span class="keyword">let</span> participants: <span class="type">Int</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Person</span>: <span class="title class_">Decodable</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> name: <span class="type">String</span></span><br><span class="line">	<span class="keyword">let</span> friends: <span class="type">Int</span></span><br><span class="line">	<span class="keyword">let</span> joined: <span class="type">Date</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是隐式地使用Swift泛型代码的第一个示例。<code>Decodable</code>如何使用泛型是比较复杂的，如果你想要深入了解，可以再阅读文章之后，自己去发掘。<br>在这里我只是想指出的它们是泛型的，即使你没有发现他。<code>Codable</code>通过泛型使事情很复杂，例如编码和解码，但是使你的代码很简单。</p>
<h1 id="四、泛化返回值"><a href="#四、泛化返回值" class="headerlink" title="四、泛化返回值"></a>四、泛化返回值</h1><p>现在，我们要解码两个JSON文件，并准备好数据来测试我们将要构建的接口。这很简单。 我们要做的就是读取每个文件中的数据，并将其提供给JSONDecoder对象。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TestData</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> events: [<span class="type">Event</span>] <span class="operator">=</span> loadEvents()</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> participants: [<span class="type">Person</span>] <span class="operator">=</span> loadParticipants()</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">loadEvents</span>() -&gt; [<span class="type">Event</span>] &#123;</span><br><span class="line">		<span class="keyword">let</span> url <span class="operator">=</span> <span class="type">Bundle</span>.main.url(forResource: <span class="string">&quot;Events&quot;</span>, withExtension: <span class="string">&quot;json&quot;</span>)<span class="operator">!</span></span><br><span class="line">		<span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try!</span> <span class="type">Data</span>(contentsOf: url)</span><br><span class="line">		<span class="keyword">let</span> decoder <span class="operator">=</span> <span class="type">JSONDecoder</span>()</span><br><span class="line">		decoder.dateDecodingStrategy <span class="operator">=</span> .secondsSince1970</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">try!</span> decoder.decode([<span class="type">Event</span>].<span class="keyword">self</span>, from: data)</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">loadParticipants</span>() -&gt; [<span class="type">Person</span>] &#123;</span><br><span class="line">		<span class="keyword">let</span> url <span class="operator">=</span> <span class="type">Bundle</span>.main.url(forResource: <span class="string">&quot;Participants&quot;</span>, withExtension: <span class="string">&quot;json&quot;</span>)<span class="operator">!</span></span><br><span class="line">		<span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try!</span> <span class="type">Data</span>(contentsOf: url)</span><br><span class="line">		<span class="keyword">let</span> decoder <span class="operator">=</span> <span class="type">JSONDecoder</span>()</span><br><span class="line">		decoder.dateDecodingStrategy <span class="operator">=</span> .secondsSince1970</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">try!</span> decoder.decode([<span class="type">Person</span>].<span class="keyword">self</span>, from: data)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里有一大堆的重复代码，所以我想尽可能的重用、泛化这些代码。<br>泛化这两种方法的第一行很容易。 我们需要的是每个文件名称的String参数。 以下几行是相同的，因此我们在那里没有任何问题。<br>但是最后一行是不容易泛化的。在那里，我们指明那种模型作为返回值。<br>但是，如何才能泛化方法的返回值？<br>Swift提供了Any类型，可以表示任何类型。 我们可以尝试使用这种方法来加载事件和参与者的单一方法，但这并不是一个很好的解决方案。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TestData</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> events: [<span class="type">Event</span>] <span class="operator">=</span> readFile(named: <span class="string">&quot;Events&quot;</span>) <span class="keyword">as!</span> [<span class="type">Event</span>]</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> participants: [<span class="type">Person</span>] <span class="operator">=</span> readFile(named: <span class="string">&quot;Participants&quot;</span>) <span class="keyword">as!</span> [<span class="type">Person</span>]</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">readFile</span>(<span class="params">named</span> <span class="params">name</span>: <span class="type">String</span>) -&gt; [<span class="keyword">Any</span>]  &#123;</span><br><span class="line">		<span class="keyword">let</span> url <span class="operator">=</span> <span class="type">Bundle</span>.main.url(forResource: name, withExtension: <span class="string">&quot;json&quot;</span>)<span class="operator">!</span></span><br><span class="line">		<span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try!</span> <span class="type">Data</span>(contentsOf: url)</span><br><span class="line">		<span class="keyword">let</span> decoder <span class="operator">=</span> <span class="type">JSONDecoder</span>()</span><br><span class="line">		decoder.dateDecodingStrategy <span class="operator">=</span> .secondsSince1970</span><br><span class="line">		<span class="keyword">if</span> <span class="keyword">let</span> events <span class="operator">=</span> <span class="keyword">try?</span> decoder.decode([<span class="type">Event</span>].<span class="keyword">self</span>, from: data) &#123;</span><br><span class="line">			<span class="keyword">return</span> events</span><br><span class="line">		&#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="keyword">let</span> participants <span class="operator">=</span> <span class="keyword">try?</span> decoder.decode([<span class="type">Person</span>].<span class="keyword">self</span>, from: data) &#123;</span><br><span class="line">			<span class="keyword">return</span> participants</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> []</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在这里，我们尝试使用两种模型类型对文件进行解码。 如果我们得到一些数据，那么它是正确的类型。 否则，我们尝试下一个。<br>上面的代码能够工作，但是有一些问题。</p>
<ul>
<li>每当我们向应用程序添加新的可解码模型类型时，我们都必须扩展条件语句。 （如果你很好奇，则违反了开放式封闭原则）。</li>
<li>使用<code>Any</code>作为返回类型将擦除类型信息。 每次调用<code>readFile（named :)</code>时，都必须使用<code>as</code>将其结果转换为所需的类型</li>
<li>编译器无法帮助我们。 我们可以将readFile（named :)的结果转换为任何类型。 如果我们犯了一个错误，我们只会在我们的应用崩溃时发现它（可能是在用户手中）。</li>
</ul>
<h1 id="五、使用泛型来参数化函数中的类型"><a href="#五、使用泛型来参数化函数中的类型" class="headerlink" title="五、使用泛型来参数化函数中的类型"></a>五、使用泛型来参数化函数中的类型</h1><p>我们终于达到了OOP方法的极限。<br>在<code>readFile(named:)</code>函数中，我们不但需要参数来指定函数的要打开的文件，同时需要一个类型来制定函数的返回值。<br>我们希望能够说出<code>readFile（named :)</code>返回的类型是<code>[Event]</code>还是<code>[Person]</code>，就像我们对常规参数所做的那样。<br>显然有一种方法可以做到这一点。 我们已经将类型参数传递给JSONDecoder的encode（_：from :)方法。<br>实际上，这就是泛型的。 同样，即使你不知道泛型，我们也会使用泛型。 但是现在，我们看到了泛型是什么：它们使我们也可以将类型用作参数，而不仅仅是值。<br>在Swift中，您可以使用尖括号在函数名称后立即声明泛型。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TestData</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> events: [<span class="type">Event</span>] <span class="operator">=</span> readFile(named: <span class="string">&quot;Events&quot;</span>)</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> participants: [<span class="type">Person</span>] <span class="operator">=</span> readFile(named: <span class="string">&quot;Participants&quot;</span>)</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">readFile</span>&lt;<span class="type">ModelType</span>&gt;(<span class="params">named</span> <span class="params">name</span>: <span class="type">String</span>) -&gt; [<span class="type">ModelType</span>]  &#123;</span><br><span class="line">		<span class="keyword">let</span> url <span class="operator">=</span> <span class="type">Bundle</span>.main.url(forResource: name, withExtension: <span class="string">&quot;json&quot;</span>)<span class="operator">!</span></span><br><span class="line">		<span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try!</span> <span class="type">Data</span>(contentsOf: url)</span><br><span class="line">		<span class="keyword">let</span> decoder <span class="operator">=</span> <span class="type">JSONDecoder</span>()</span><br><span class="line">		decoder.dateDecodingStrategy <span class="operator">=</span> .secondsSince1970</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">try!</span> decoder.decode([<span class="type">ModelType</span>].<span class="keyword">self</span>, from: data)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意<br>这里的代码依然有问题，后续我们将讲到如何解决。</p>
</blockquote>
<p>一些开发人员使用单个字母（例如T，U等）来命名其泛型，但我觉得这太难读了。尽可能尝试使用有意义的名称，这也是Swift标准库的做法。 在这种情况下，此方法处理模型类型，因此ModelType是比M更好的名称。<br>在方法中声明泛型后，可以将其用作：</p>
<ul>
<li>返回类型（我们的情况）；</li>
<li>任何参数的类型；</li>
<li>任何局部常量&#x2F;变量的类型；</li>
<li>另一个通用函数的参数。<br>我们的ModelType泛型充当了我们不知道的类型的占位符。 仅当使用方法时才确定该类型。<br>你可以在对<code>readFile（named :)</code>的两次调用中看到这一点。 在那里，我们不再需要强制类型转换。 <code>TestData</code>结构的事件和参与者静态属性具有显式类型。 由于类型推断，Swift编译器可以确定在每次调用中使用哪种类型。<br>这样，编译器就可以进行所有必要的检查，并在类型不匹配时向您发出警告。 如果使用Any，则不会发生某些事情。<br>正如我提到的，此代码尚不能编译。 编译器现在在抱怨我们的代码中的一些问题。</li>
</ul>
<h1 id="六、限制泛型的选项，并使用类型约束确保正确性"><a href="#六、限制泛型的选项，并使用类型约束确保正确性" class="headerlink" title="六、限制泛型的选项，并使用类型约束确保正确性"></a>六、限制泛型的选项，并使用类型约束确保正确性</h1><p>我们的方法还不能编译，原因是<code>ModelType</code>太“泛型”了。<br>目前，我们可以使用任何类型的<code>readFile（named :)</code>方法。 尽管这为我们提供了很大的灵活性，但并不是我们应用程序中的所有类型都可解码。<br>这里的问题是：<code>JSONDecoder的encode（_：from :)</code>方法只需要符合<code>Decodable</code>的类型。 否则，解码器无法将JSON数据映射到类型的属性。为了约束可与泛型一起使用的类型，我们使用类型约束。 这些仅将泛型限制为源自特定类或符合一个特定协议的类型。在我们的情况下，我们希望ModelType泛型符合Decodable。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TestData</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> events: [<span class="type">Event</span>] <span class="operator">=</span> readFile(named: <span class="string">&quot;Events&quot;</span>)</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">let</span> participants: [<span class="type">Person</span>] <span class="operator">=</span> readFile(named: <span class="string">&quot;Participants&quot;</span>)</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">func</span> <span class="title function_">readFile</span>&lt;<span class="type">ModelType</span>: <span class="type">Decodable</span>&gt;(<span class="params">named</span> <span class="params">name</span>: <span class="type">String</span>) -&gt; [<span class="type">ModelType</span>]  &#123;</span><br><span class="line">		<span class="keyword">let</span> url <span class="operator">=</span> <span class="type">Bundle</span>.main.url(forResource: name, withExtension: <span class="string">&quot;json&quot;</span>)<span class="operator">!</span></span><br><span class="line">		<span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try!</span> <span class="type">Data</span>(contentsOf: url)</span><br><span class="line">		<span class="keyword">let</span> decoder <span class="operator">=</span> <span class="type">JSONDecoder</span>()</span><br><span class="line">		decoder.dateDecodingStrategy <span class="operator">=</span> .secondsSince1970</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">try!</span> decoder.decode([<span class="type">ModelType</span>].<span class="keyword">self</span>, from: data)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>现在我们的代码可以工作了。 编译器知道我们将用于泛型的任何类型都符合Decodable。 如果我们尝试使用不使用的类型，则编译器将阻止我们犯错误。<br>使用Any时也不会发生这种情况。 在这种情况下，编译器没有有关基础类型的信息。<br>现在我们可以看到编译器阻止了我们，因为<code>decode（_：from :)</code>方法也是一个泛型函数。 当你在Foundation框架的头文件中查看其声明时，这一点很明显。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">open</span> <span class="keyword">class</span> <span class="title class_">JSONDecoder</span> &#123;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">// ...</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">open</span> <span class="keyword">func</span> <span class="title function_">decode</span>&lt;<span class="type">T</span>&gt;(<span class="keyword">_</span> <span class="params">type</span>: <span class="type">T</span>.<span class="keyword">Type</span>, <span class="params">from</span> <span class="params">data</span>: <span class="type">Data</span>) <span class="keyword">throws</span> -&gt; <span class="type">T</span> <span class="keyword">where</span> <span class="type">T</span> : <span class="type">Decodable</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>在这里，你可以看到在泛型上声明类型约束的另一种方法。 T的可分解约束位于方法声明后面的where子句中，而不是在泛型的声明中。</p>
<h1 id="七、Swift标准库中的泛型"><a href="#七、Swift标准库中的泛型" class="headerlink" title="七、Swift标准库中的泛型"></a>七、Swift标准库中的泛型</h1><p>在我们的<code>TestData</code>结构中，只有<code>readFile（named :)</code>方法是通用的。 但是泛型也可以用于整个类型，而不仅仅是功能。<br>Swift标准库的常规<code>Array</code>和<code>Dictionary</code>数据结构就是很好的例子。 这是泛型在日常代码中清晰可见的另一种情况。<br>你可以将任何类型的值放入数组和字典中。 一旦确定了集合内容的类型，编译器便可以检查所有值是否都属于同一类型。<br>既然我们已经了解了泛型的工作原理，那么你可以理解为什么集合会以这种方式工作。 同样，你可以检查它们的声明并查看它们是否使用泛型。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@frozen</span> <span class="keyword">public</span> <span class="keyword">struct</span> <span class="title class_">Array</span>&lt;<span class="title class_">Element</span>&gt; &#123;</span><br><span class="line">	<span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">@frozen</span> <span class="keyword">public</span> <span class="keyword">struct</span> <span class="title class_">Dictionary</span>&lt;<span class="title class_">Key</span>, <span class="title class_">Value</span>&gt; <span class="title class_">where</span> <span class="title class_">Key</span> : <span class="title class_">Hashable</span> &#123;</span><br><span class="line">	<span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Array和Dictionary的Element和Value泛型没有任何类型约束。 因此，你可以将任何东西放入集合中。<br>Dictionary类型还具有带有Hashable类型约束的Key泛型。 这是因为字典是一个哈希表，使用哈希函数存储其内容。<br>Swift中还有另一种泛型类型——可选类型。<br>Swift出色地完成了将可选内容隐藏在大量语法糖后面的工作。 您使用？声明了可选参数。 运算符，使用nil表示不存在的值，并使用？，??和！展开可选项。 您也可以在条件语句中使用条件绑定，即let或guard let。<br>但是，在幕后，可选选项只不过是包含两种情况的泛型枚举。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@frozen</span> <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">Optional</span>&lt;<span class="title class_">Wrapped</span>&gt; : <span class="title class_">ExpressibleByNilLiteral</span> &#123;</span><br><span class="line">	<span class="keyword">case</span> none</span><br><span class="line">	<span class="keyword">case</span> <span class="keyword">some</span>(<span class="type">Wrapped</span>)</span><br><span class="line">	<span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>none</code>情况表示一个nil值，而<code>some</code>情况包含一个值（如果存在）。 由于可选对象还需要使用任何类型，因此可选枚举使用了无类型约束的Wrapped泛型。<br>这意味着，通过可选，你可以使用任何与枚举一起使用的Swift构造。</p>
<h1 id="八、先写具体的代码，发现需要之后，再改写为泛型"><a href="#八、先写具体的代码，发现需要之后，再改写为泛型" class="headerlink" title="八、先写具体的代码，发现需要之后，再改写为泛型"></a>八、先写具体的代码，发现需要之后，再改写为泛型</h1><p>数组，词典和可选变量是你在许多文章中发现的典型的泛型类型示例。 同样，它们向你展示了泛型是如何工作的，但是它们并不能帮助你了解如何在代码中使用它们。<br>集合是必要且可理解的编程概念。 但是泛型在其他不太明显的类型中也很有用。<br>我们的应用程序用户界面将提供一个很好的例子。从设计中我们已经知道事件列表和参与者列表具有相同的结构。 尽管很明显他们需要共享代码，但最好还是先开始编写特定的代码。<br>仅在明显需要将哪些内容归纳之后，才对代码进行归纳。 直接从通用代码开始通常会导致过度优化。虽然通常在性能上下文中使用该概念，但通常可以将其扩展到代码编写的有效性。在达到限制之前，你将不知道需要对代码的哪些部分进行泛化。<br>通常，开发人员不必要地编写通用代码。 他们认为，在某些时候，他们的代码将需要与几种类型一起使用。 但是实际上，大多数代码仅在一个特定实例中使用。<br>因此，不要仅仅因为某些将来可能永远不会发生的假设用例，而使你的代码难以阅读。 仅在需要通用代码时才通用代码。<br>我们可以从“联接”按钮为表行创建表，这是一个仅需基本参数的简单视图。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">RowButton</span>: <span class="title class_">View</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> title: <span class="type">String</span></span><br><span class="line">	<span class="keyword">let</span> color: <span class="type">Color</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> body: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">Text</span>(title)</span><br><span class="line">			.font(.subheadline)</span><br><span class="line">			.bold()</span><br><span class="line">			.foregroundColor(.white)</span><br><span class="line">			.padding(<span class="type">EdgeInsets</span>(top: <span class="number">8.0</span>, leading: <span class="number">16.0</span>, bottom: <span class="number">8.0</span>, trailing: <span class="number">16.0</span>))</span><br><span class="line">			.background(color)</span><br><span class="line">			.cornerRadius(<span class="number">20</span>)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">EventsView_Previews</span>: <span class="title class_">PreviewProvider</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> previews: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">VStack</span>(spacing: <span class="number">8.0</span>) &#123;</span><br><span class="line">			<span class="type">RowButton</span>(title: <span class="string">&quot;Join&quot;</span>, color: .orange)</span><br><span class="line">			<span class="type">RowButton</span>(title: <span class="string">&quot;Message&quot;</span>, color: .blue)</span><br><span class="line">		&#125;</span><br><span class="line">		.padding()</span><br><span class="line">		.previewLayout(.sizeThatFits)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<img src="/2022/12/09/%E6%8E%8C%E6%8F%A1Swift%E6%B3%9B%E5%9E%8B%EF%BC%9A%E4%B8%80%E4%B8%AA%E5%AE%9E%E9%99%85%E7%9A%84%E4%BB%A3%E7%A0%81%E9%87%8D%E7%94%A8%E7%A4%BA%E4%BE%8B/preview.png" class="" title="预览">
<p>这样，我们可以为表中的事件行创建一个视图，并使用该视图创建事件的完整列表。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">EventsView</span>: <span class="title class_">View</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> events: [<span class="type">Event</span>]</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> body: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">NavigationView</span> &#123;</span><br><span class="line">			<span class="type">List</span>(events) &#123; event <span class="keyword">in</span></span><br><span class="line">				<span class="type">EventRow</span>(event: event)</span><br><span class="line">			&#125;</span><br><span class="line">			.navigationBarTitle(<span class="string">&quot;Events&quot;</span>)</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">EventRow</span>: <span class="title class_">View</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> event: <span class="type">Event</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> body: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">HStack</span>(spacing: <span class="number">16.0</span>) &#123;</span><br><span class="line">			<span class="type">Image</span>(event.title)</span><br><span class="line">				.resizable()</span><br><span class="line">				.frame(width: <span class="number">70.0</span>, height: <span class="number">70.0</span>)</span><br><span class="line">				.cornerRadius(<span class="number">10.0</span>)</span><br><span class="line">			<span class="type">VStack</span>(alignment: .leading, spacing: <span class="number">4.0</span>) &#123;</span><br><span class="line">				<span class="type">Text</span>(event.title)</span><br><span class="line">					.font(.headline)</span><br><span class="line">				<span class="type">Group</span> &#123;</span><br><span class="line">					<span class="type">Text</span>(event.date.formatted(.full))</span><br><span class="line">					<span class="type">Text</span>(<span class="string">&quot;<span class="subst">\(event.participants)</span> people going&quot;</span>)</span><br><span class="line">				&#125;</span><br><span class="line">				.font(.subheadline)</span><br><span class="line">				.foregroundColor(.secondary)</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="type">Spacer</span>()</span><br><span class="line">			<span class="type">RowButton</span>(title: <span class="string">&quot;Join&quot;</span>, color: .orange)</span><br><span class="line">		&#125;</span><br><span class="line">		.padding(.vertical, <span class="number">16.0</span>)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">EventsView_Previews</span>: <span class="title class_">PreviewProvider</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> previews: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">Group</span> &#123;</span><br><span class="line">			<span class="type">EventsView</span>(events: <span class="type">TestData</span>.events)</span><br><span class="line">			<span class="type">VStack</span>(spacing: <span class="number">8.0</span>) &#123;</span><br><span class="line">				<span class="type">RowButton</span>(title: <span class="string">&quot;Join&quot;</span>, color: .orange)</span><br><span class="line">				<span class="type">RowButton</span>(title: <span class="string">&quot;Message&quot;</span>, color: .blue)</span><br><span class="line">			&#125;</span><br><span class="line">			.padding()</span><br><span class="line">			.previewLayout(.sizeThatFits)</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<img src="/2022/12/09/%E6%8E%8C%E6%8F%A1Swift%E6%B3%9B%E5%9E%8B%EF%BC%9A%E4%B8%80%E4%B8%AA%E5%AE%9E%E9%99%85%E7%9A%84%E4%BB%A3%E7%A0%81%E9%87%8D%E7%94%A8%E7%A4%BA%E4%BE%8B/preview2.png" class="" title="This is an image">
<h1 id="九、自定义协议来约束泛型类型"><a href="#九、自定义协议来约束泛型类型" class="headerlink" title="九、自定义协议来约束泛型类型"></a>九、自定义协议来约束泛型类型</h1><p>现在我们有了一些可以分析的实际代码，我们可以将其概括化以与Event和Person类型一起使用。<br>这也是我们需要参数化类型的情况。 我们知道我们需要用通用类来代替Event类型，但是仅那条信息并不能使我们走得太远。<br>问题出在代码的某些部分中，这些部分使用了Event类型的特定属性。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">EventRow</span>: <span class="title class_">View</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> event: <span class="type">Event</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> body: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">HStack</span>(spacing: <span class="number">16.0</span>) &#123;</span><br><span class="line">			<span class="type">Image</span>(event.title)</span><br><span class="line">				.resizable()</span><br><span class="line">				.frame(width: <span class="number">70.0</span>, height: <span class="number">70.0</span>)</span><br><span class="line">				.cornerRadius(<span class="number">10.0</span>)</span><br><span class="line">			<span class="type">VStack</span>(alignment: .leading, spacing: <span class="number">4.0</span>) &#123;</span><br><span class="line">				<span class="type">Text</span>(event.title)</span><br><span class="line">					.font(.headline)</span><br><span class="line">				<span class="type">Group</span> &#123;</span><br><span class="line">					<span class="type">Text</span>(event.date.formatted(.full))</span><br><span class="line">					<span class="type">Text</span>(<span class="string">&quot;<span class="subst">\(event.participants)</span> people going&quot;</span>)</span><br><span class="line">				&#125;</span><br><span class="line">				.font(.subheadline)</span><br><span class="line">				.foregroundColor(.secondary)</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="type">Spacer</span>()</span><br><span class="line">			<span class="type">RowButton</span>(title: <span class="string">&quot;Join&quot;</span>, color: .orange)</span><br><span class="line">		&#125;</span><br><span class="line">		.padding(.vertical, <span class="number">16.0</span>)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>人员类型没有标题，日期和参与者属性。在我们的特定示例中，我们可以重命名Person的属性以匹配这些名称，但这仍然无济于事。<br>我们添加到视图中的任何泛型都将独立于Event和Person类型。 它们具有共同的属性并不重要。 使用泛型，无论如何我们都无法访问它们中的任何一个。<br>此外，这还是一个不好的做法，因为一个人没有头衔或参与者。 在其他情况下，你可能会有无法匹配的类型。<br>每次我们需要对泛型进行假设时，都需要使用类型约束。 不过，在这种情况下，我们没有可以使用的协议。<br>解决方案是创建一个自定义的。我们在表格行中显示的任何类型都必须具有标题，两个子标题，图像等。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protocol</span> <span class="title class_">TableItem</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> navigationTitle: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> actionName: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> buttonColor: <span class="type">Color</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line"> </span><br><span class="line">	<span class="keyword">var</span> headline: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">	<span class="keyword">var</span> imageName: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">	<span class="keyword">var</span> subheadline1: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">	<span class="keyword">var</span> subheadline2: <span class="type">String</span> &#123; <span class="keyword">get</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>请注意，我在TableItem协议中同时使用了常规属性和静态属性。 这是因为headline，imageName，subheadline1和subheadline2是随每个值更改的属性，但是对特定类型的所有值，navigationTitle，actionName和buttonColor保持不变。</p>
<h1 id="十、使用类型约束使泛型具体化"><a href="#十、使用类型约束使泛型具体化" class="headerlink" title="十、使用类型约束使泛型具体化"></a>十、使用类型约束使泛型具体化</h1><p>现在我们有了定义行要求的协议，我们可以将其用作泛型的类型约束。<br>一旦泛型受到约束，你就可以将其视为该协议的实例，因为您知道编译器将强制执行其要求。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Row</span>&lt;<span class="title class_">Item</span>: <span class="title class_">TableItem</span>&gt;: <span class="title class_">View</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> item: <span class="type">Item</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> body: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">HStack</span>(spacing: <span class="number">16.0</span>) &#123;</span><br><span class="line">			<span class="type">Image</span>(item.imageName)</span><br><span class="line">				.resizable()</span><br><span class="line">				.frame(width: <span class="number">70.0</span>, height: <span class="number">70.0</span>)</span><br><span class="line">				.cornerRadius(<span class="number">10.0</span>)</span><br><span class="line">			<span class="type">VStack</span>(alignment: .leading, spacing: <span class="number">4.0</span>) &#123;</span><br><span class="line">				<span class="type">Text</span>(item.headline)</span><br><span class="line">					.font(.headline)</span><br><span class="line">				<span class="type">Group</span> &#123;</span><br><span class="line">					<span class="type">Text</span>(item.subheadline1)</span><br><span class="line">					<span class="type">Text</span>(item.subheadline2)</span><br><span class="line">				&#125;</span><br><span class="line">				.font(.subheadline)</span><br><span class="line">				.foregroundColor(.secondary)</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="type">Spacer</span>()</span><br><span class="line">			<span class="type">RowButton</span>(title: <span class="type">Item</span>.actionName, color: <span class="type">Item</span>.buttonColor)</span><br><span class="line">		&#125;</span><br><span class="line">		.padding(.vertical, <span class="number">16.0</span>)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>EventsView</code>类型包含<code>EventRow</code>视图，我们将其更改为<code>Row &lt;Item&gt;</code>泛型类型。 包含通用类型的任何类型都必须为通用类型指定一种类型，或者也必须公开该通用类型。 在这里，我们需要第二种选择。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TableView</span>&lt;<span class="title class_">Item</span>: <span class="title class_">TableItem</span> &amp; <span class="title class_">Identifiable</span>&gt;: <span class="title class_">View</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> items: [<span class="type">Item</span>]</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> body: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">NavigationView</span> &#123;</span><br><span class="line">			<span class="type">List</span>(items) &#123; item <span class="keyword">in</span></span><br><span class="line">				<span class="type">Row</span>(item: item)</span><br><span class="line">			&#125;</span><br><span class="line">			.navigationBarTitle(<span class="type">Item</span>.navigationTitle)</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>TableView类型的Item泛型也必须符合Identifiable协议，因为List视图需要这样做。 在Swift中，你可以使用＆运算符在类型声明中编写协议。<br>最后一步是使我们的模型类型同时符合TableItem和Identifiable协议。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Event</span>: <span class="title class_">Decodable</span>, <span class="title class_">Identifiable</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> title: <span class="type">String</span></span><br><span class="line">	<span class="keyword">let</span> date: <span class="type">Date</span></span><br><span class="line">	<span class="keyword">let</span> participants: <span class="type">Int</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> id: <span class="type">String</span> &#123; title &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">extension</span> <span class="title class_">Event</span>: <span class="title class_">TableItem</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> navigationTitle: <span class="type">String</span> &#123; <span class="string">&quot;Events&quot;</span> &#125;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> actionName: <span class="type">String</span> &#123; <span class="string">&quot;Join&quot;</span> &#125;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> buttonColor: <span class="type">Color</span> &#123; .orange &#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> headline: <span class="type">String</span> &#123; title &#125;</span><br><span class="line">	<span class="keyword">var</span> imageName: <span class="type">String</span> &#123; title &#125;</span><br><span class="line">	<span class="keyword">var</span> subheadline1: <span class="type">String</span> &#123; date.formatted(.full) &#125;</span><br><span class="line">	<span class="keyword">var</span> subheadline2: <span class="type">String</span> &#123; <span class="string">&quot;<span class="subst">\(participants)</span> people going&quot;</span> &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Person</span>: <span class="title class_">Decodable</span>, <span class="title class_">Identifiable</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> name: <span class="type">String</span></span><br><span class="line">	<span class="keyword">let</span> friends: <span class="type">Int</span></span><br><span class="line">	<span class="keyword">let</span> joined: <span class="type">Date</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> id: <span class="type">String</span> &#123; name &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">extension</span> <span class="title class_">Person</span>: <span class="title class_">TableItem</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> navigationTitle: <span class="type">String</span> &#123; <span class="string">&quot;Participants&quot;</span> &#125;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> actionName: <span class="type">String</span> &#123; <span class="string">&quot;Message&quot;</span> &#125;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> buttonColor: <span class="type">Color</span> &#123; .blue &#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">var</span> headline: <span class="type">String</span> &#123; name &#125;</span><br><span class="line">	<span class="keyword">var</span> imageName: <span class="type">String</span> &#123; name &#125;</span><br><span class="line">	<span class="keyword">var</span> subheadline1: <span class="type">String</span> &#123; <span class="string">&quot;<span class="subst">\(friends)</span> friends&quot;</span> &#125;</span><br><span class="line">	<span class="keyword">var</span> subheadline2: <span class="type">String</span> &#123; <span class="string">&quot;Joined <span class="subst">\(joined.formatted(.long))</span>&quot;</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用Swift扩展使我们能够满足TableItem的要求，而不必以任何方式更改Event和Person类型。多亏了此添加，编译器现在允许我们在TableView泛型结构中使用这两种类型。</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">TableView_Previews</span>: <span class="title class_">PreviewProvider</span> &#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">var</span> previews: <span class="keyword">some</span> <span class="type">View</span> &#123;</span><br><span class="line">		<span class="type">Group</span> &#123;</span><br><span class="line">			<span class="type">TableView</span>(items: <span class="type">TestData</span>.events)</span><br><span class="line">			<span class="type">TableView</span>(items: <span class="type">TestData</span>.participants)</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<img src="/2022/12/09/%E6%8E%8C%E6%8F%A1Swift%E6%B3%9B%E5%9E%8B%EF%BC%9A%E4%B8%80%E4%B8%AA%E5%AE%9E%E9%99%85%E7%9A%84%E4%BB%A3%E7%A0%81%E9%87%8D%E7%94%A8%E7%A4%BA%E4%BE%8B/preview3.png" class="" title="This is an image">
<h1 id="十一、总结"><a href="#十一、总结" class="headerlink" title="十一、总结"></a>十一、总结</h1><p>Swift泛型是一种强大的语言功能，它使我们能够以其他方式无法实现的方式来抽象代码。</p>
<p>多亏了泛型，我们不仅可以将值用作参数，而且可以将类型用作类型。 此外，Swift编译器由于其自动类型推断功能，可以将泛型与类型匹配，从而检查我们代码的正确性。</p>
<p>但是请注意，与任何其他高级功能一样，泛型会使您的代码更难以理解。</p>
<p>除非有必要，否则不要接使用泛型。 在过早地使泛型，这是过早优化的一种情况，这会使你的代码不必要地变得复杂。 始终从具体的代码开始，并且仅在遇到需要它的具体情况时才对其进行泛化。</p>
]]></content>
      <categories>
        <category>swift</category>
      </categories>
      <tags>
        <tag>swift</tag>
        <tag>泛型</tag>
      </tags>
  </entry>
  <entry>
    <title>预处理(宏)——想说爱你不容易</title>
    <url>/2022/12/08/%E9%A2%84%E5%A4%84%E7%90%86-%E5%AE%8F-%E2%80%94%E2%80%94%E6%83%B3%E8%AF%B4%E7%88%B1%E4%BD%A0%E4%B8%8D%E5%AE%B9%E6%98%93/</url>
    <content><![CDATA[<h1 id="一、工作中遇到的怪事"><a href="#一、工作中遇到的怪事" class="headerlink" title="一、工作中遇到的怪事"></a>一、工作中遇到的怪事</h1><p>在编写代码是，发现了一个崩溃：</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// XXXX.m</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> MOU_GE_HONG_DING_YI == 1</span></span><br><span class="line">        _library = <span class="comment">// 涉密，删除代码；从路径A获取文件</span></span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">        <span class="comment">// 崩溃发生在这里</span></span><br><span class="line">        _library = <span class="comment">// 涉密，删除代码；从路径B获取文件</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>


<p>但是没有改动到这里，却发生了崩溃！</p>
<ol>
<li>经分析，初步判断可能是需要走if分支，但是却走到了else分支</li>
<li>发现在路径A确实存在文件、而路径B却不存在这个文件</li>
<li>那么问题就发生在编译XXXX.m文件时，宏MOU_GE_HONG_DING_YI尚未被定义</li>
</ol>
<span id="more"></span>

<h1 id="二、问题详解"><a href="#二、问题详解" class="headerlink" title="二、问题详解"></a>二、问题详解</h1><h2 id="2-1-代码展示"><a href="#2-1-代码展示" class="headerlink" title="2.1 代码展示"></a>2.1 代码展示</h2><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 宏在此文件中定义</span></span><br><span class="line"><span class="comment">// Header.h</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MOU_GE_HONG_DING_YI 1</span></span><br></pre></td></tr></table></figure>

<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 宏在此文件中使用</span></span><br><span class="line"><span class="comment">//  XXXX.m</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> MOU_GE_HONG_DING_YI == 1</span></span><br><span class="line">        _library = <span class="comment">// 涉密，删除代码；从路径A获取文件</span></span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">        <span class="comment">// 崩溃发生在这里</span></span><br><span class="line">        _library = <span class="comment">// 涉密，删除代码；从路径B获取文件</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<h2 id="2-2-大胆猜测"><a href="#2-2-大胆猜测" class="headerlink" title="2.2 大胆猜测"></a>2.2 大胆猜测</h2><p>根据问题表现，猜测是编译器在预处理XXXX.m文件时，其尚未了解到有MOU_GE_HONG_DING_YI的宏定义，所以就把if分支的代码去掉了，只保留了else分支的代码。</p>
<h2 id="2-3-小心求证"><a href="#2-3-小心求证" class="headerlink" title="2.3 小心求证"></a>2.3 小心求证</h2><h3 id="2-3-1-复现问题"><a href="#2-3-1-复现问题" class="headerlink" title="2.3.1 复现问题"></a>2.3.1 复现问题</h3><p>为了验证自己的猜测，编写如下测试代码；</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Person.h</span></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> : <span class="title">NSObject</span></span></span><br><span class="line">+ (<span class="type">void</span>)testMacro;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// Person.m</span></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Person</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// DEBUG 是Xcode预置的</span></span><br><span class="line"><span class="comment">// THIS_IS_A_TEST_MACRO 在Person的子类Student类中声明</span></span><br><span class="line"></span><br><span class="line">+ (<span class="type">void</span>)testMacro &#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> DEBUG</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Person Define DEBUG&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Person NO Define DEBUG&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    </span><br><span class="line"><span class="meta">#<span class="keyword">if</span> THIS_IS_A_TEST_MACRO == 1</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Person Define THIS_IS_A_TEST_MACRO&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Person NO THIS_IS_A_TEST_MACRO&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// Student.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> THIS_IS_A_TEST_MACRO 1</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Student</span> : <span class="title">Person</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="type">void</span>)StudentTestMacro;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// Student.m</span></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Student</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="type">void</span>)StudentTestMacro &#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> DEBUG</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Student Define DEBUG&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Student NO Define DEBUG&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    </span><br><span class="line"><span class="meta">#<span class="keyword">if</span> THIS_IS_A_TEST_MACRO == 1</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Student Define THIS_IS_A_TEST_MACRO&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Student NO THIS_IS_A_TEST_MACRO&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    </span><br><span class="line">    [<span class="variable language_">super</span> testMacro];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// main.m</span></span><br><span class="line"><span class="type">int</span> main(<span class="type">int</span> argc, <span class="keyword">const</span> <span class="type">char</span> * argv[]) &#123;</span><br><span class="line">    [Student StudentTestMacro];</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">2022-05-19 14:51:49.164576+0800 cmdProject[75822:301165] Student Define DEBUG</span></span><br><span class="line"><span class="comment">2022-05-19 14:51:49.165422+0800 cmdProject[75822:301165] Student Define THIS_IS_A_TEST_MACRO</span></span><br><span class="line"><span class="comment">2022-05-19 14:51:49.165453+0800 cmdProject[75822:301165] Person Define DEBUG</span></span><br><span class="line"><span class="comment">2022-05-19 14:51:49.165474+0800 cmdProject[75822:301165] Person NO THIS_IS_A_TEST_MACRO</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>
<ol>
<li>由于Student继承自Person，所以编译器优先处理Person的相关代码</li>
<li>在对Person进行预处理的时候，尚未了解到THIS_IS_A_TEST_MACRO宏已被定义，所以至保留了else分支</li>
</ol>
<p>通过代码验证了之前的猜测——某些预处理与文件的依赖有关系，如果宏定义在子孙文件里，则父文件的预处理会出乎预料</p>
<h2 id="2-2-2-代码实证"><a href="#2-2-2-代码实证" class="headerlink" title="2.2.2 代码实证"></a>2.2.2 代码实证</h2><h3 id="2-2-2-1-XXXX-m原代码预处理"><a href="#2-2-2-1-XXXX-m原代码预处理" class="headerlink" title="2.2.2.1 XXXX.m原代码预处理"></a>2.2.2.1 XXXX.m原代码预处理</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// XXXX.i</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 可见在原始代码中，由于引入了Header.h，所有走了if分支</span></span><br><span class="line">    # <span class="number">1</span> <span class="string">&quot;...../common/Header.h&quot;</span> <span class="number">1</span></span><br><span class="line"></span><br><span class="line">     _library = <span class="comment">// 涉密，删除代码；从路径A获取文件</span></span><br></pre></td></tr></table></figure>
<h3 id="2-2-2-2-修改XXXX-m后代码预处理"><a href="#2-2-2-2-修改XXXX-m后代码预处理" class="headerlink" title="2.2.2.2 修改XXXX.m后代码预处理"></a>2.2.2.2 修改XXXX.m后代码预处理</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// XXXX.i</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 可见在原始代码中，由于引入了Header.h，所有走了else分支</span></span><br><span class="line">     _library = <span class="comment">// 涉密，删除代码；从路径B获取文件</span></span><br></pre></td></tr></table></figure>
<h1 id="三、最佳实现"><a href="#三、最佳实现" class="headerlink" title="三、最佳实现"></a>三、最佳实现</h1><h2 id="3-1-不推荐使用宏"><a href="#3-1-不推荐使用宏" class="headerlink" title="3.1 不推荐使用宏"></a>3.1 不推荐使用宏</h2><p>绝大部分的程序员都听说过，不要使用宏。它有许多问题：</p>
<ol>
<li>丢失类型</li>
<li>可能包含隐蔽错误（如加的小括号过少）</li>
<li>不支持调试</li>
<li>在预处理阶段被处理掉，重要信息丢失（语法、语义等后续阶段的编译处理无法获取宏信息）</li>
<li>…….<br>宏的不好，不一而足，以上仅列举部分</li>
</ol>
<h2 id="3-2-不得不使用宏"><a href="#3-2-不得不使用宏" class="headerlink" title="3.2 不得不使用宏"></a>3.2 不得不使用宏</h2><p>宏就像“渣男、渣女”一样，除了渣，别的地方都很好！</p>
<img src="/2022/12/08/%E9%A2%84%E5%A4%84%E7%90%86-%E5%AE%8F-%E2%80%94%E2%80%94%E6%83%B3%E8%AF%B4%E7%88%B1%E4%BD%A0%E4%B8%8D%E5%AE%B9%E6%98%93/zhanan.png" class="" title="渣男">
<p>扯远啦！<br>宏虽然有万般不好，但是许多地方仍然被使用！除了其他的一些宏编写的最佳时间外，推荐大家在XCode的工程配置里定义宏（除非你非常确定宏的应用范围在且只在当前文件范围内），这样就可以规避此问题。</p>
]]></content>
      <categories>
        <category>基础</category>
      </categories>
      <tags>
        <tag>宏</tag>
      </tags>
  </entry>
</search>
